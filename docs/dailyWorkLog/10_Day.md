# 10일차 작업 로그 (2026-08-10)

## 회차 요약

- 활성 워크스트림: 저장소 계층 · 화면 · 크롤 파이프라인(유휴 배정)
- 이번 회차 배치 근거: 9일차에 **015A**가 닫히면서 그것 하나가 동시에 막고 있던 두 갈래가 함께 열렸다 —
  저장소 계층 **017**(`의존: 007·015A`)과 화면 **016A**(`의존: 015A·009A`). 크롤 파이프라인은 016B가
  016A를 기다려 열린 Task가 없어 유휴 배정을 받았다.
- 결과: 이슈 1건 발견 / 전건 해소, 신규 등재 4건(I-019~I-022), 전체 테스트 통과
- **이 회차로 "크롤을 걸고 그 결과를 읽어 오는" 양쪽이 동시에 섰다.** 홈에서 언론사를 골라 실행할 수
  있게 됐고(016A), 그렇게 쌓인 결과를 화면이 읽을 조회 API가 생겼다(017). Phase 4가 열렸다.
- **9일차에 세 에이전트가 산출만 하고 보고 없이 침묵했던 문제가 이번에도 한 번 재현됐다** — 화면
  워크스트림이 보고 없이 유휴로 넘어갔고, 팀장이 즉시 보고를 재요청해 회수했다. 9일차처럼 팀장이 대신
  검증하는 상황으로 번지지는 않았고, **워크스트림 간 상호 교차검증이 정상적으로 두 쌍 모두 수행됐다.**

## 워크스트림별 완료 내역

### 저장소 계층 (01.저장소계층.md)

- 완료 Task: **017** · 수집 결과 조회 API
- 산출물: `app/api/runs/route.ts` · `app/api/runs/[runId]/route.ts` ·
  `app/api/runs/[runId]/articles/route.ts` · `app/api/runs/[runId]/articles/[articleId]/route.ts` ·
  `lib/api/run-format.ts` + `.test.ts` · `lib/api/article-search.ts` + `.test.ts`
- 비고: **포맷·검색 로직을 라우트에서 빼 순수 함수로 만들고 vitest로 고정했다**(D-031). 셀렉터 라벨은
  서버가 조립하고 클라이언트는 받은 문자열을 그대로 쓴다. 대상 언론사 N+1 조회는 `listPress()` 1회 +
  `Map`으로 피했다. **`skippedCount`를 실패에 합산하지 않는다** — 라벨 조립 함수는 그 값을 입력으로
  받지도 않게 만들어 회귀를 구조적으로 막았다(D-029).

### 화면 (03.화면.md)

- 완료 Task: **016A** · 크롤링 실행 화면(홈) — 언론사 선택 카드 및 실행 옵션
- 산출물: `app/page.tsx`(`ScreenPlaceholder` 제거) · `components/crawl/press-select-card.tsx` ·
  `components/crawl/crawl-run-panel.tsx` · `lib/api/crawl-client.ts`
- 비고: **`ScreenPlaceholder`가 걷힌 네 번째 화면이자 앱의 진입점이다.** 설계서 상태 ①②⑥⑦을 구현했고
  ③④⑤⑧은 016B 몫으로 남겼다. 설계서 스켈레톤의 `ScrollArea` 리터럴이 모바일 분기를 빠뜨린 것을
  담당이 발견해 모바일-퍼스트로 바로잡았다(`h-[320px] sm:h-[420px]`) — 리뷰가 "설계서 자체의 버그를
  올바르게 고친 것"으로 판정했다.

### 크롤 파이프라인 (02.크롤파이프라인.md) — 유휴 배정

- 완료 Task: 없음(열린 Task 없음). 유휴 배정 「016B 상태 7종을 화면 설계서 01과 대조」 수행
- 산출물: 상태 7종 대조 표 · 중단 상태 결정 초안(→ D-030) · 신규 이슈 1건(→ I-022)
- 비고: **대조가 실제 결함 하나를 잡았다** — `recoverRunProgress`가 언론사별 상태를 `done`/`waiting`
  둘로만 복원해서, 서버가 재시작되면 **완전히 실패한 언론사가 "대기"로 보인다**(I-022). 설계서 §⑤가
  요구하는 `CircleX` + 실패 사유를 만들 데이터 자체가 없다. 코드를 읽는 리뷰가 아니라 **화면이 받을
  데이터를 설계서와 한 줄씩 맞춰 봐야** 드러나는 종류였다.

## 교차검증 결과

- **크롤 파이프라인 → 저장소 계층(017)**: 6개 검증 항목 **전건 pass**. 실측으로 재현했다 — 200건 합성
  run을 직접 만들어 40~58ms 확인, 언론사를 임시 제거해 삭제 케이스 확인, **손상된 기사 파일을 실제로
  만들어** D-032의 우회가 손상을 404로 둔갑시키지 않고 500으로 올린다는 것을 확인. 검증용 임시 변경이
  전부 원복됐는지도 대조했다.
- **저장소 계층 → 화면(016A)**: 7개 항목 중 **1건 fail** → 수정 후 재검증 **5/5 pass**.
- 리뷰 상대는 각 영역 문서 「영역 정의」 표의 `리뷰 상대` 행을 그대로 따랐다.

## DoD 충족 현황

| Task | DoD 충족 | 미충족 항목 |
| --- | --- | --- |
| 017 | 4/4 | 없음 |
| 016A | 원 Task 016 DoD 6개 중 **2개 충족 · 4개는 016B 대기** | ③진행률 갱신 · ④`[수집 결과 보기]` 이동 · ⑤중단 버튼 · ⑥전체 동선 — 전부 016B가 만드는 패널에 걸려 있다 |

- Playwright MCP 동선(017): `/results`에서 fetch로 목록 → 요약 → 기사목록(+`?q=` 검색) → 기사 본문을
  순서대로 호출해 응답 스키마 확인. 404 케이스(없는 `runId`·없는 `articleId`·경로 순회 시도)도 함께 태웠다.
- Playwright MCP 동선(016A): 홈 진입 → 언론사 4곳 렌더·RSS/HTML 배지·`sourceUrl` 확인 → 2곳 체크 →
  전체 선택 `aria-checked="mixed"`·"2/4개 선택됨" 확인 → 최대 기사 수 2로 변경 → `[크롤링 시작]` →
  `POST /api/crawl` **202** + `runId: 20260810-213251` 수신 확인 → 실행 중 입력·체크박스 `disabled` 확인.
  실크롤은 RSS 2곳 × 2건으로 수 초 내 종료(`successCount: 4`, txt 4건 저장). **375/768/1280 세 폭**
  확인, `browser_console_messages` 에러 **0건**.
- **016A의 ⑥언론사 0건 빈 상태는 코드 경로 대조로 판정했다.** 활성 언론사 4곳이 실제로 등록돼 있고
  `press-sources.json`을 지우면 다른 워크스트림의 검증 자산을 무너뜨리기 때문이다. 리뷰가 조건 분기와
  `EmptyState` props를 설계서 문구와 대조해 성립을 확인했다.

## 발견·해결한 이슈

1. [화면] **Task 016 파일 분할 경계 미준수** — ③④ 실행 패널(옵션 입력·시작 버튼 포함)이 통째로
   `app/page.tsx`에 인라인이었고 `crawl-run-panel.tsx`가 없었다. 설계서 §파일 분할 경계와 **D-006**
   ("B가 `page.tsx`를 다시 고치는 형태로 만들지 않는다")·**D-011**("A가 B의 컴포넌트를 정적 뼈대째
   만들어 두고 B가 내부를 채운다")에 정면으로 어긋난다. 이대로 두면 016B가 새 컴포넌트를 만드는 데
   더해 `app/page.tsx`를 다시 열어 인라인 블록을 걷어내야 했다.
   → `components/crawl/crawl-run-panel.tsx`로 분리하고 `app/page.tsx`는 호출 한 줄만 남겼다.
   ③④⑤ 자리는 한국어 TODO 주석으로 위임 지점을 명시했다. (재검증 저장소 계층 **pass**)
   - **이 fail의 원인 절반은 팀장의 소환 프롬프트다.** "016B 파일을 만들지 마라"라고 적었는데 그것이
     D-011과 어긋났다. 담당의 판단 착오가 아니라 지시가 틀린 것이라 그렇게 기록한다.
2. [크롤 파이프라인 → 신규 등재] **I-022** — 서버 재시작 후 복구된 진행 상태는 언론사 전체 실패를
   표현할 수 없다(`recoverRunProgress`가 `failed`로 복원하는 경로 자체가 없다). 016B 착수를 막지는
   않으며, D-030 결정 4가 "복구 스냅샷에서는 언론사별 상세를 그리지 않고 런 레벨 안내로 대체한다"로
   우회를 확정했다.
3. [저장소 계층 → 신규 등재] **I-020** — `readArticle`이 "파일 없음"과 "메타 손상"을 같은 `Error`로
   던져 404/500을 가를 수 없다. Task 007 소유 파일이라 고치지 않고 `fs.access` 사전 확인으로 우회했다(D-032).
4. [크롤 파이프라인 발견 → 저장소 계층 등재] **I-021** — `assertSafeSegment`가 일반 `Error`를 던져
   경로 순회 시도가 라우트마다 다른 상태 코드(500 / 404)로 응답한다. **순회 차단과 정보 유출 방어는
   완전하고 문제는 상태 코드뿐**이며, `app/api/crawl/[runId]/*`도 같은 증상이라 017이 만든 결함이 아니다.
   두 영역 라우트를 함께 손봐야 해서 이번 회차에서 닫지 않았다.
5. [화면 → 9일차 이월분 해소] **I-019** — 중단으로 끝난 실행을 그리는 화면 상태가 설계서 01에 없었다.
   016B 담당(크롤 파이프라인)이 **D-030**으로 결론을 내고 팀장이 설계서에 §⑧ 중단됨을 반영했다.

## 팀장 전체 테스트 (항상 실행)

- npm run lint: 통과 (에러·경고 0건)
- npm run typecheck: 통과 (`next typegen && tsc --noEmit`)
- npm run test: 통과 (15개 파일 · **163개 케이스** — 9일차 150건에서 +13, `run-format` 8 · `article-search` 5)
- npm run build: 통과. 라우트 **15종**(정적 6 · 동적 9) — `/api/runs` 계열 4종 신규

## 문서 갱신

- docs/ROADMAP/work/*.md 상태 마커: `01.저장소계층.md` Task **017** · `03.화면.md` Task **016A**
- docs/ROADMAP.md: 완료 체크 **017**. 016은 016A만 끝나 체크하지 않고 `- **진행 메모**:` 한 줄만 남겼다.
  진행률 **Phase 4: 1/2(🟡 진행 중)** · 합계 **18/25 (72%)**.
  Task 016 구현 규칙의 "상태 7종"을 **8종**으로 정정(D-030).
- docs/ROADMAP/work/*.md 영역 정의: 변경 없음
- docs/screens/01-crawl-run.md: **§⑧ 중단됨 신설**(문안·`Square` 아이콘·토스트·파생 판정) ·
  §③ 응답 필드 목록에 `recovered` 추가와 복구 스냅샷 안내 · 완료 조각 제목에 `aborted` 재사용 명시 ·
  스켈레톤 `crawlStatus` 주석을 5종 전체로 정정. **전부 D-030을 근거로 팀장이 반영했다**(워크스트림은
  설계서를 직접 고치지 않는다).
- docs/ISSUES.md: **I-019**(중단 화면 상태 부재 — 해결됨) · **I-020**(`readArticle` 타입 구분) ·
  **I-021**(`assertSafeSegment` 상태 코드) · **I-022**(복구 경로 실패 표현 불가) 등재
- docs/DECISIONS.md: **D-029**(실패와 요청하지 않음을 타입으로 가른다 — 9일차 마감 후 작업분) ·
  **D-030**(중단 상태 ⑧ 설계) · **D-031**(포맷 함수 위치 `lib/api/`) · **D-032**(`fs.access` 우회) 등재.
  draft 5건 병합 후 삭제

## 다음 회차에 열리는 Task

- **Task 016B**(크롤 파이프라인) — 진행·완료·부분 실패 패널. `의존: 016A·015B` 충족. **D-030과 설계서
  §⑧이 문안까지 확정해 두었고 `crawl-run-panel.tsx` 뼈대도 서 있다.**
- **Task 018A**(화면) — 수집 결과 화면 실행 선택·요약 카드. `의존: 017` 충족
- **Task 021A**(저장소 계층) — 키워드 분석 실행 및 캐시 기록. `의존: 020B·017` 충족
- 세 영역 모두 열린 Task가 있어 유휴가 없다.

### 인계 사항

- **016B**: `crawl-run-panel.tsx`에 `onReset` 콜백이 없다. 설계서 §④의 `[새로 크롤링하기]`는 `runId`를
  `null`로, `selectedIds`를 빈 `Set`으로 되돌려야 하는데 그 상태를 `app/page.tsx`가 쥐고 있다. props에
  한 줄 추가하는 통상적 확장이라 D-006이 막는 재구조화가 아니다 — 리뷰가 미리 짚어 두었다.
  `useCrawlProgress(runId)`는 `runId`가 이미 prop이라 패널 안에서 바로 호출할 수 있다.
- **018A**: `docs/screens/02-collect-result.md`의 마크업 스켈레톤이 `targetPressNames: string[]`처럼
  실제 API 타입과 다른 더미 인터페이스를 쓴다. **스켈레톤을 그대로 베끼면 컴파일 단계에서 어긋난다** —
  017이 확정한 응답 스키마를 기준으로 타입을 새로 잡아야 한다. `docs/run-api-schema.draft.md`도 참고하되
  실제 구현이 단일 소스다.

## git

- 브랜치: day-10 (day-9에서 분기)
- 커밋: 회차 마감 커밋 1건
- 푸시: 12일차까지 마친 뒤 사용자 확인 후 결정
