# 12일차 작업 로그 (2026-08-11)

## 회차 요약

- 활성 워크스트림: 화면 · 저장소 계층 · 크롤 파이프라인(유휴 배정)
- 이번 회차 배치 근거: 11일차에 **018A**와 **021A**가 닫히면서 각자의 B 조각이 열렸다 —
  **018B**(`의존: 018A`) · **021B**(`의존: 021A`). 크롤 파이프라인은 022A가 021B·012B를, 023이 화면
  5종 전부를 기다려 열린 Task가 없어 유휴 배정을 받았다.
- 결과: 이슈 0건 발견(**교차검증 두 쌍 모두 fail 0**), 신규 등재 1건(I-026), 전체 테스트 통과
- **원 Task 018과 021이 함께 완성되면서 Phase 4가 닫혔다.** 남은 미완료는 Phase 5의 022와 Phase 6의
  023·024·025 넷뿐이고, `ScreenPlaceholder`가 남은 화면은 `/keywords` 하나다.
- **"크롤 → 저장 → 확인 → 분석"이 처음으로 끝까지 이어졌다.** 홈에서 언론사를 골라 크롤을 걸고,
  수집 결과 화면에서 기사 본문을 읽고, 그 run의 키워드를 조건 걸어 꺼내는 것이 전부 동작한다.
  마지막으로 남은 것은 그 결과를 그리는 화면이다.

### 회차 중단과 재개

**작업 도중 세 워크스트림 에이전트가 사용량 한도로 동시에 끊겼다.** 화면이 I-025 수정에 착수해
`lib/api/run-format.ts`에 포맷터 두 개를 추가한 상태였고 커밋되지 않은 채였다. 한도가 풀린 뒤
**새 에이전트 세 개를 띄워 워크로그(10·11일차)를 읽혀 맥락을 이어붙였고, 워킹 트리에 남은 미완성
변경도 지우지 않고 그대로 이어 완성했다.** 회차 산출물에 손실은 없다 — 다만 새 에이전트는 앞 회차의
대화 맥락을 갖지 못하므로 소환 프롬프트에 선행 산출물과 계약을 전부 명시해야 했다.

## 워크스트림별 완료 내역

### 화면 (03.화면.md)

- 완료 Task: **018B** · 수집 결과 화면 — 기사 파일 목록 및 본문 미리보기 (**원 Task 018 완성**)
- 산출물: `components/results/article-file-list.tsx` · `components/results/article-preview.tsx`(둘 다
  뼈대 내부 구현) · `components/results/run-summary-card.tsx`(I-023·I-025) ·
  `lib/api/run-format.ts`(`formatLocalTimeOnly`·`formatLocalTime`) · `lib/api/run-format.test.ts`(회귀 5건)
- 비고: **`app/results/page.tsx`를 한 줄도 고치지 않았다.** 11일차에 018A가 선택 상태 채널까지 뚫어 둔
  덕분이고, D-006이 요구한 형태가 실제로 성립한다는 것이 이번에 증명됐다. 11일차에 지적받은 가짜 데이터
  문제도 재발하지 않았다(`grep MOCK_ components/` 무매치).

### 저장소 계층 (01.저장소계층.md)

- 완료 Task: **021B** · 키워드 분석 API 및 결과 캐시 — 조회 API 및 필터·재분석 (**원 Task 021 완성**)
- 산출물: `app/api/runs/[runId]/keywords/route.ts` · `lib/keyword/filter-keywords.ts` + `.test.ts`(10건) ·
  `lib/keyword/keywords-response.ts` + `.test.ts`(5건)
- 비고: **캐시 경계를 코드로 못 박았다** — 필터는 캐시 위에서만 돌고 `keywords.json`에 구워지지 않으며,
  `summary`는 필터에 흔들리지 않는다. 후자는 `keywords-response.test.ts`가 **참조 동일성(`toBe`)까지**
  확인해 고정한다. 틀려도 화면이 멀쩡해 보이는 종류라 여기서 잡지 못하면 아무도 못 잡는다.

### 크롤 파이프라인 (02.크롤파이프라인.md) — 유휴 배정

- 완료 Task: 없음(열린 Task 없음). **022A·023 착수 준비**
- 산출물: 설계서 03 §①~⑥ 대조 표 · `RunOption` 타입 이슈(→ I-026) · I-018 처리 범위 결정(→ D-039) ·
  교차검증 2건 중 1건(021B)
- 비고: **10일차와 같은 방식이 다시 값을 냈다.** 설계서 03 §⑥(분석 진행 상태)이 "021B가 만들지 않는
  폴링 API를 요구하는가"라는 의심을 확인해 **ROADMAP 구현 규칙이 이미 "3단계 스텝 표시는 클라이언트가
  그린다"로 판정해 뒀음**을 찾아냈고, I-018의 죽은 범위가 `runCrawl` + 배치 봉투 스키마 2개뿐이며
  단건 검증 스키마는 `fetchHtml`이 계속 쓴다는 것을 import 그래프로 확인했다.

## 교차검증 결과

- **저장소 계층 → 화면(018B)**: 8항목 **전건 pass**. 담당이 실측하지 못한 **삭제된 언론사 배지**를
  리뷰가 `press-sources.json`을 임시 변경해 직접 태우고 원복했다(세 컴포넌트 모두 정상 렌더 확인).
- **크롤 파이프라인 → 저장소 계층(021B)**: 9항목 **전건 pass**. 리뷰가 두 가지를 더 했다 —
  ① 담당이 데이터가 없어 건너뛴 **기사 0건 run**을 기존 자산을 건드리지 않고 임시 디렉터리로 만들어
  직접 태워 닫았고, ② 담당이 "022B가 정할 남은 판단"으로 남긴 **`ratio` 분모**가 설계서에 이미 답이
  있음을 mock 숫자 역산으로 찾아냈다(D-037).
- **이번 회차는 두 리뷰 모두 fail이 0이었다.** 10·11일차에 각각 1건·4건이 나왔던 것과 대비되는데,
  그 회차들에서 지적된 패턴(뼈대만으로는 부족하고 props 채널까지 필요하다 · 가짜 데이터를 그리지
  않는다 · 설계서 스켈레톤 타입을 베끼지 않는다)이 이번 소환 프롬프트에 명시적으로 들어간 결과다.

## DoD 충족 현황

| Task | DoD 충족 | 미충족 항목 |
| --- | --- | --- |
| 018B | 원 Task 018 DoD **5/5**(018A+018B 합산) | 없음. ④의 **셀렉터 프리셀렉트 확인은 규정대로 Task 022 완료 후로 유예**(이동 자체는 확인). ②실행 이력 0건과 로딩 스켈레톤은 실데이터를 지울 수 없어 코드 경로로 판정했고 리뷰가 정당하다고 확인 |
| 021B | 원 Task 021 DoD **5/5**(021A+021B 합산) | 없음. ⑤(Playwright MCP)는 `/keywords` 화면이 아직 없어 **실서버 HTTP 동선으로 대체**했다 — 021A 회차에 확립한 방식이다 |

- Playwright MCP 동선(018B): `/results` 진입 → 파일 클릭 선택(`aria-selected="true"` + `bg-muted`) →
  검색 "한화"(53→2건, `browser_network_requests`로 `?q=`가 실제로 서버에 나가는 것 확인) → 검색 결과
  0건 → 검색 초기화 → 실행 전환(요약·목록·미리보기 동시 갱신) → **키보드 Enter로 행 선택**(마우스 없는
  접근성 동선) → `[키워드 분석]` → `/keywords?runId=...` 도착. **375/768/1280 세 폭**, 콘솔 에러 0건.
- 021B 동선: 최초 분석 → 필터 변경 → 불용어 추가 → `?force=true` 강제 재분석을 실서버에 순서대로 태웠다.
- **본문 개행 보존을 computed `white-space: pre-wrap`으로 확인했다** — 013A가 문단 개행을 살려 저장한
  것이 화면까지 이어지는지 처음으로 눈으로 확인한 지점이다.

### 실측 — 키워드 조회 API (021B)

run `20260810-204917`(기사 53건, 전체 키워드 2412개) 기준이다.

| 요청 | `items` | `summary` |
| --- | ---: | --- |
| (필터 없음) | 2412 | 기준값 |
| `?minCount=3` | 672 | **동일** |
| `?pos=NNP` | 358 (전부 NNP) | **동일** |
| `?pos=NNG,NNP` | 2229 | **동일** |
| `?topN=10` | 10 | **동일** |
| `?minCount=5&pos=NNG&topN=5` | 5 | **동일** |

- **응답 시간**: 캐시 히트 8~50ms · 최초 분석 1875ms · `force=true` 재분석 549~597ms
  (프로세스 내 Kiwi가 이미 `build()`된 상태라 최초보다 빠르다 — 019의 `globalThis` 싱글턴이 실제로
  동작한다는 증거다).
- **불용어 반영**: "보안"(57회) 추가 → `force=true` 재분석에서 사라지고 `stopwordExcludedCount`가
  84→141로 **정확히 +57**. 확인 후 원복해 원래 값으로 완전히 복귀했다.
- **함정 ② 회귀 없음**: 재분석 후에도 `공개(8) 적용(8) 발표(7)`가 그대로 살아 있고 상위 5개도
  `AI(96) 보안(57) 반도체(45) 기술(43) 연구(39)`로 11일차 값과 일치한다.
- **400 케이스**: `minCount=abc` · `pos=XX` · `topN=0`이 각각 필드별 한국어 메시지로 거부된다.

## 발견·해결한 이슈

1. [화면] **I-023 해소** — 요약 카드에 `skippedCount`를 "N건 미수집"으로 노출했다. **`41건 미수집`을
   `<span>` 밖 평문으로 두어 `text-destructive` 색 상속 자체가 구조적으로 불가능하게** 만들었다.
   `skippedCount === 0`이면 문구가 렌더되지 않는다.
2. [화면] **I-025 해소** — `toTimeOnly()`의 `slice(11, 16)` 결합을 걷어내고
   `lib/api/run-format.ts`의 `formatLocalTimeOnly`·`formatLocalTime`으로 교체했다. `grep "toTimeOnly\|slice(11"
   components/` 무매치로 확인. **회귀 테스트에
   `formatLocalDateTimeSecond(iso).endsWith(formatLocalTime(iso))` 교차 확인을 넣어** 한쪽 포맷이 바뀌면
   실제로 깨지게 했다 — 이슈가 지적한 "자동으로 잡을 방법이 없는 자리"에 그물을 친 것이다.
3. [크롤 파이프라인 → 신규 등재] **I-026** — 설계서 03 스켈레톤의 `RunOption`이 Task 017이 확정한
   `RunListItem`을 다시 더미로 선언한다. **컴파일이 깨지지 않아 더 위험하다** — 베끼면 `status`로
   미완료 run을 거르거나 `startedAt`으로 정렬을 보정할 자리가 타입에서 사라진다. I-024와 같은 계열이다.
4. [교차검증이 닫은 것] **기사 0건 run 실측** — 담당이 데이터가 없어 vitest로만 고정했던 것을 리뷰가
   임시 디렉터리로 직접 태워 확인했다(200 + `summary` 전부 0 + `message`). `message` 필드가 022A의 빈
   상태 UI를 좌우하므로 실측 없이 넘길 항목이 아니었다.
5. [교차검증이 닫은 것] **`ratio` 분모 확정** — 담당이 "022B 몫의 열린 판단"으로 남긴 것을 리뷰가
   설계서 `03-hot-keyword.md:441`의 주석(`0~1, 최상위 키워드 대비 비중`)과 mock 숫자
   (`96/128 = 0.75`, `84/128 ≈ 0.66`)로 역산해 **이미 닫힌 사양**임을 밝혔다(D-037).

## 팀장 전체 테스트 (항상 실행)

- npm run lint: 통과 (에러·경고 0건)
- npm run typecheck: 통과 (`next typegen && tsc --noEmit`)
- npm run test: 통과 (18개 파일 · **192개 케이스** — 11일차 172건에서 +20,
  `filter-keywords` 10 · `keywords-response` 5 · `run-format` 5)
- npm run build: 통과. 라우트 **16종**(정적 6 · 동적 10) — `ƒ /api/runs/[runId]/keywords` 신규

## 문서 갱신

- docs/ROADMAP/work/*.md 상태 마커: `03.화면.md` **018B** · `01.저장소계층.md` **021B**
- docs/ROADMAP.md: 완료 체크 **018**·**021**(각각 두 조각이 모두 끝나 원 Task를 접었다).
  진행률 **Phase 4: 2/2(✅ 완료)** · **Phase 5: 3/4(🟡 진행 중)** · 합계 **21/25 (84%)**.
  Phase 4 제목에 ✅ 부착
- docs/ROADMAP/work/*.md 영역 정의: 변경 없음
- docs/screens/*: 변경 없음(이번 회차에 설계서를 고칠 사유가 없었다)
- docs/ISSUES.md: **I-026**(설계서 03의 `RunOption` 더미 선언) 등재
- docs/DECISIONS.md: **D-035**(`pos` 쉼표 구분) · **D-036**(`force` 문자열 비교) ·
  **D-037**(`rank`·`ratio` 미포함, 분모는 1위 count) · **D-038**(키워드 응답 형태) ·
  **D-039**(I-018 제거 범위) 등재. draft 3건 병합 후 삭제

## 다음 회차에 열리는 Task

- **Task 022A**(크롤 파이프라인) — 핫 키워드 분석 화면의 조건 바·요약 KPI·진행 표시.
  `의존: 021B·012B` 충족. **이번 회차 유휴 배정으로 설계서 대조를 이미 끝냈고 막는 항목이 없다.**
- **Task 022B**(저장소 계층) — 랭킹 표·Top 5 카드. `의존: 021B·012B` 충족.
  **`ratio` 분모가 D-037로 확정돼 열린 판단이 없다.**
- 화면은 024가 023을 기다려 **열린 Task가 없다** — 유휴다. `03.화면.md`의 24~25주차 유휴 항목
  (Task 025 검증 시나리오 사전 리허설 · 375/768/1280 반응형 점검 준비)이 배정 대상이다.
- **022A·022B가 끝나면 018B DoD ④(분석 화면 셀렉터 프리셀렉트)의 되돌아오는 검증을 함께 태운다.**
  `01.저장소계층.md` Task 022B 항목이 그렇게 걸어 두었다.

### 인계 사항

- **022A**: `RunOption`을 새로 선언하지 말고 `lib/api/run-client.ts`의 `RunListItem`·`fetchRuns()`를
  그대로 import해라(**I-026**). `AnalysisSummary`·`KeywordRankItem`·`PosTag` 세 타입은 설계서
  스켈레톤을 그대로 옮겨도 안전하다 — 실제 스키마와 한 글자도 안 어긋난다.
  §⑥ 진행 상태는 **확정값 없는 `Progress`로 그린다** — 분석은 요청 안에서 끝나고 폴링 API가 없다.
- **022B**: `rank = index + 1`, `ratio = count / items[0].count`(**D-037**). API는 이 둘을 채우지 않는다.
- **023**: 제거 범위가 **D-039**로 정리돼 있다. ROADMAP Task 023 「생성/수정 파일」에
  `lib/crawler/{run.ts, types.ts, index.ts, press-crawler.ts}` 4건을 추가해야 범위가 맞다.
- **`data/runs/`에 실행이 21건 쌓였다.** 검증 자산이라 지우지 않았지만, Task 025(전체 여정 수동 검증)는
  `data/`를 비운 완전 초기 상태에서 시작해야 하므로 그 회차 전에 정리 시점을 정해야 한다.

## git

- 브랜치: day-12 (day-11에서 분기)
- 커밋: 회차 마감 커밋 1건
- 푸시: 사용자 확인 후 결정
