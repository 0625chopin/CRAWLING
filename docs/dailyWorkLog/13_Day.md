# 13일차 작업 로그 (2026-08-11)

## 회차 요약

- 활성 워크스트림: 크롤 파이프라인 · 저장소 계층 · 화면(유휴 배정)
- 이번 회차 배치 근거: 12일차에 **021B**가 닫히면서 마지막 화면인 핫 키워드 분석이 열렸다 —
  **022A**(`의존: 021B·012B`) · **022B**(같은 의존). 화면은 024가 023을 기다려 열린 Task가 없어
  유휴 배정을 받았다.
- 결과: **교차검증 두 쌍 모두 fail 0**, 신규 등재 이슈 4건(I-027~I-030) · 결정 4건(D-040~D-043),
  전체 테스트 통과
- **원 Task 022가 완성되면서 Phase 5가 닫혔다.** 진행률은 22/25(88%)이고 남은 것은 Phase 6의
  023·024·025 셋뿐이다.
- **`ScreenPlaceholder`를 쓰는 라우트가 0이 됐다.** 5개 화면이 전부 실제 구현이다 — 이것이 Task 023의
  마지막 선행 조건이었다.
- **"크롤 → 저장 → 확인 → 분석 → 랭킹"이 처음으로 화면에서 끝까지 이어졌다.** PRD의 F001~F008이
  모두 화면에 존재한다.

### 회차 운영에서 드러난 것

**이번 회차의 실제 비용은 구현이 아니라 검증 환경이었다.** 세 워크스트림이 각각 독립적으로 같은
증상(라우트 404, 탭 하이재킹, 필터값이 저절로 바뀜)을 보고했고, 원인은 둘이었다 — 낡은 `next start`
서버가 3000번을 잡고 있었던 것(**I-028**)과 Playwright MCP 브라우저가 세 워크스트림에 공유되는
것(**I-029**)이다. 전자는 회차 중간에 해소했고 후자는 열린 채로 남는다. **Task 025는 다른 워크스트림이
브라우저를 쓰지 않는 회차에 단독으로 태워야 한다.**

## 워크스트림별 완료 내역

### 크롤 파이프라인 (02.크롤파이프라인.md)

- 완료 Task: **022A** · 핫 키워드 분석 화면 — 조건 바·요약 KPI·진행 표시
- 산출물: `app/keywords/page.tsx`(전면 재작성) · `components/keywords/analysis-filter-bar.tsx` ·
  `components/keywords/analysis-summary.tsx` · `components/keywords/analysis-progress.tsx` ·
  `lib/api/keyword-client.ts`
- 비고: **props 계약을 코드로 확정해 draft에 남기고 022B에 넘겼다.** 11일차에 "뼈대만으로는 부족하고
  props 채널까지 필요하다"고 지적받은 패턴이 이번엔 착수 전에 반영됐고, 그 결과 **022B가 `page.tsx`
  호출부를 한 줄도 고치지 않고 붙였다.** 세 컴포넌트 자리에 로컬 플레이스홀더를 두되 import 3줄 교체로
  갈아끼울 수 있는 형태로 설계한 것이 주효했다.
- 비고: **DoD ②를 스스로 "부분 충족"으로 보고했다.** 실측이 문구에 못 미친다는 사실을 뭉뚱그리지 않고
  올린 판단이 옳았다 — 리뷰가 독립 재현해 같은 값을 얻었고 I-027로 굳었다.

### 저장소 계층 (01.저장소계층.md)

- 완료 Task: **022B** · 핫 키워드 분석 화면 — 랭킹 표·카드·Top 5 (**원 Task 022 완성**)
- 산출물: `components/keywords/top-keyword-cards.tsx` · `components/keywords/keyword-rank-table.tsx` ·
  `components/keywords/keyword-rank-card-list.tsx` · `app/keywords/page.tsx`(import 3줄 + 플레이스홀더
  블록 삭제만)
- 비고: **`--chart-*` 함정을 피했다.** 다크 모드에서 `getComputedStyle`로 실측해 1위→5위가 배경에
  스며들며 옅어지는 것을 확인했다(순위 인코딩 반전 없음). 라이트 모드만 보고 넘기면 못 잡는 종류다.
- 비고: **DoD ⑤를 근거 없이 체크했다.** 본문에는 "화면 직접 유도는 안 함, 코드 확인만"이라고 적어 두고
  체크박스는 `[x]`였다. 교차검증이 이를 잡아 실측으로 닫았다. DoD ②도 같은 실측치를 022A와 달리
  "충족"으로 적었고 재현 결과 022A 쪽이 정확했다.

### 화면 (03.화면.md) — 유휴 배정

- 완료 Task: 없음(열린 Task 없음). **018B 되돌아오는 검증 완료 · Task 025 검증 자산 정비**
- 산출물: `docs/screens/verification-checklist.draft.md`(§0 스냅샷·§1부 0~6단계·R단계·§2부 반응형 표
  전면 갱신) · `docs/screens/playwright-scenarios.draft.md`(016A/016B 커버리지 정정) · 교차검증 1건(022A)
- 비고: **10·12일차와 같은 방식이 또 값을 냈다.** 6일차 초안이 홈·수집 결과를 아직
  `ScreenPlaceholder`라고 적고 있었고 소스 테스트(010B)도 "대기"로 남아 있었다. 코드와 대조해 전부
  정정했고, 빈 상태 아이콘이 `Newspaper`가 아니라 `Inbox`인 것, 펼침 표기 "▸"가 실제로는
  `ChevronRight`/`ChevronDown` 아이콘인 것까지 실물 기준으로 박아 넣었다.
- 비고: **025 실행 가능 항목이 11개(6일차) → 58/62개(이번 회차)로 올랐다.** 남은 4개는 막힌 것이
  아니라 이번에 안 눌러 본 것이다(4-7 본문 표현 확인 · 4-11 2회차 분석 속도 스톱워치 · R-4 키보드 전용 ·
  R-5 다크모드 토글).

## 교차검증 결과

- **화면 → 크롤 파이프라인(022A)**: DoD 6개 + 구현 규칙 + I-026 회귀 + CONVENTIONS + 설계서 대조 +
  App Router까지 **전 항목 pass**. 리뷰가 두 가지를 더 했다 — ① 담당이 "위험하다"며 코드로만 확인한
  **DoD ⑤(run 0건 빈 상태)를 `data/runs` 임시 개명으로 직접 태워 닫고 원복**했고, ② **DoD ②를 독립
  재현**해 17,915→12,080(-32.57%)이 담당 보고와 일치함을 확인한 뒤 `aggregate.ts:15-32`를 읽어
  코드 결함이 아님을 판정했다.
- **크롤 파이프라인 → 저장소 계층(022B)**: props 계약 · D-037 · Top 5 색 차등 · 비중 막대 · 정렬 UI ·
  CONVENTIONS · 설계서 대조 **전 항목 pass**. 리뷰가 세 가지를 더 했다 — ① `git diff`로 **`page.tsx`가
  허용 범위(import 3줄 + 플레이스홀더 삭제)를 넘어 훼손되지 않았음**을 확인했고, ② **DoD ⑤를
  `page.route()`로 `/api/runs`를 빈 배열로 가로채는 다른 방식으로 한 번 더** 닫았으며, ③ **`ratio` 분모가
  "필터 적용 후 집합의 1위"임을 실측으로 확정**했다(SL 품사를 해제하면 "보안"이 100%가 된다 — D-037의
  전제와 일치하는 의도된 동작).
- **되돌아오는 검증(018B DoD ④) — 화면**: `03.화면.md`가 "022B 완료 회차에 함께 태운다"로 걸어 둔
  마지막 미확인 항목을 닫았다. **목록 첫 항목이 아닌 세 번째 run**으로 태워 프리셀렉트가 우연이 아님을
  갈랐고, `[분석 시작]`까지 눌러 기사 수(65건)가 선택한 run과 일치함을 확인했다. **pass.**
- **이번 회차도 두 리뷰 모두 fail이 0이었다** — 12일차에 이어 두 회차 연속이다. 다만 이번엔 **리뷰가
  담당의 자기 점검 구멍 두 개를 메웠다**(근거 없이 체크된 DoD ⑤, 서로 엇갈린 DoD ② 판정). fail 0이
  "볼 것이 없었다"는 뜻은 아니다.

## DoD 충족 현황

| Task | DoD 충족 | 미충족 항목 |
| --- | --- | --- |
| 022A | 원 Task 022 DoD **6/6**(022A+022B 합산) | 없음. ②는 실측(-32.6%)이 문구("절반 가까이")에 못 미치나 **코드가 아니라 문구 문제**로 판정(I-027) |
| 022B | 원 Task 022 DoD **6/6**(022A+022B 합산) | 없음. ⑤는 담당이 실측하지 못해 **교차검증이 두 가지 방식으로 닫았다** |
| 018B(되돌아오는 검증) | DoD ④ **pass** | 없음. 12일차에 유예된 마지막 항목이 닫혔다 |

- Playwright MCP 동선(022A): `/keywords`(run 없이) → 조건 바 확인 → `?runId=20260810-204917` 프리셀렉트 →
  `[분석 시작]` → 요약·Top5·표 렌더 → "고객" `Ban` → 자동 재분석·토스트·수치 변화 → 원복 →
  최소 등장 횟수 500 → 결과 0건 상태 → `[필터 초기화]` → fetch 실패 주입해 실패 상태 + `[다시 시도]` →
  375px 카드 전환. 콘솔 에러 0건.
- Playwright MCP 동선(022B): `?runId=` 프리셀렉트 → 분석 → Top5(AI 96·보안 57·반도체 45·기술 43·연구 39) →
  `aria-sort="descending"` 정적 확인 → "AI" `Ban` → 84→180 · 보안이 1위(100%)로 승격 → 원복 →
  375px 카드 50장 전환 → **1280px 다크 모드에서 Top 5 상단 선이 1→5위로 옅어지는 것 육안·`getComputedStyle`
  확인**. 콘솔 에러 0건.
- Playwright MCP 동선(018B 되돌아오는 검증): `/results` → **첫 항목이 아닌 세 번째 run** 선택 →
  `[키워드 분석]`의 `href`를 `browser_evaluate`로 직접 읽어 `?runId=20260810-224051` 확인 → 클릭 →
  도착 화면 셀렉터 표시값이 정확히 그 run → `[분석 시작]` → "분석 기사: 65건"으로 데이터까지 일치.
- 반응형 실측(03 화면): 375px Top 5 **2열**, 640px 3열, 1024px 5열. 375px에서 표 `clientHeight === 0` ·
  카드 리스트 렌더 · `scrollWidth`(360) < `innerWidth`(375)로 **가로 스크롤 없음**.

### 실측 — 핫 키워드 분석 화면 (022)

run `20260810-204917`(기사 53건 · 전체 키워드 2412개) 기준이다.

| 항목 | 값 |
| --- | --- |
| 전체 토큰 수 | 17,915 |
| 조사·어미 제거 후 | 12,080 (**-32.6%**) |
| 불용어 제외 | 84 |
| 고유 키워드 수 | 2,412 |
| Top 5 | AI 96 · 보안 57 · 반도체 45 · 기술 43 · 연구 39 |

- **`Ban` 반영**: "고객"(17회) → `stopwordExcludedCount` 84→101(**정확히 +17**) ·
  "AI"(96회) → 84→180(**정확히 +96**). 둘 다 삭제 + `force=true` 재분석으로 2412/84 복원 확인.
- **`ratio` 분모**: 필터 적용 후 집합의 1위다. SL 품사를 해제해 AI를 빼면 "보안"이 100%가 된다 —
  D-037의 전제("items가 필터된 집합인지 전체 집합인지 응답만 보고 알 수 없다")를 `ratio`에도 일관 적용한
  결과이며 의도된 동작이다.
- **다크 모드 순위 인코딩**: 카드 배경 `lab(7.78…)` vs 1위 강조선 `oklab(0.92…)`. 알파 단계
  (`1 → 0.7 → 0.5 → 0.35 → 0.2`)를 따라 배경에 스며들며 옅어진다. **`--chart-*` 함정(다크에서 1위 실종 ·
  5위만 밝음) 재현 안 됨.**
- **차트 라이브러리 미도입 확인**: `package.json`·`package-lock.json` diff 없음 ·
  `components/ui/chart.tsx` 부재 · `recharts` import 0건.

## 발견·해결한 이슈

1. [크롤 파이프라인 → 신규 등재] **I-027** — Task 022 DoD ②의 "절반 가까이"가 실측 -32.6%와 어긋난다.
   담당이 자기 점검에서 뭉뚱그리지 않고 보고했고 **리뷰가 독립 재현해 같은 값**을 얻은 뒤
   `aggregate.ts:15-32`를 읽어 **코드 결함이 아님**을 확정했다. `filteredTokenCount`는 조사·어미·접미사
   3종만 빼는 값이라 30%대가 정상 범위다. 문구 정정은 Task 023 몫이다.
2. [팀장 → 신규 등재·해결] **I-028** — 3000번은 낡은 `next start`였고 진짜 `next dev`는 3100번에 있었다.
   **`curl`의 200만 보고 "개발 서버가 떠 있다"고 판단한 것이 원인**이다. 200은 서버가 살아 있다는 뜻일
   뿐 최신 코드를 서빙한다는 뜻이 아니다. 3000번을 내리고 전 워크스트림을 3100번으로 옮겼다.
3. [세 워크스트림 → 신규 등재] **I-029** — Playwright MCP 브라우저가 공유되어 탭 이동·필터값 변경·
   다크모드 토글 무시·몽키패치 소실이 반복됐다. **각 액션 직후 스냅샷 재확인**과 `page.route()` 기반
   상태 주입으로 우회해 이번 회차 산출물에는 영향이 없다. **Task 025는 단독 회차로 태워야 한다.**
4. [크롤 파이프라인 → 신규 등재·해결] **I-030** — 앞선 검증이 불용어를 지우고도 `force=true` 재분석을
   안 해 `keywords.json` 캐시에 "AI" 제외 상태가 굳어 있었다(2411/180). **불용어 목록과 캐시가 어긋나도
   화면은 멀쩡하다** — 기준값을 아는 리뷰만 잡을 수 있었다. `force=true`로 2412/84 복원.
   **"원복"의 정의를 삭제 + 재분석으로 굳혔다.**
5. [교차검증이 닫은 것] **DoD ⑤ 실측** — 담당(022A·022B) 둘 다 `data/runs`를 비우는 것이 위험해 코드
   확인으로만 넘긴 항목을, 리뷰가 **두 가지 다른 방식**(임시 개명 · `page.route()` 응답 가로채기)으로
   각각 태워 닫았다. 둘 다 원복 확인.
6. [교차검증이 닫은 것] **`page.tsx` 훼손 여부** — 022B가 남의 파일을 고치는 작업이라 `git diff`로
   범위를 직접 확인했다. 허용 범위(import 3줄 + 플레이스홀더 블록 + 미사용 import 정리)를 넘지 않았다.
7. [화면 → 팀장 판정] **D-043** — 설계서 03이 Top 5 모바일 레이아웃을 네 곳에서 두 가지로 지시한다.
   **2열 줄바꿈**으로 판정했다(마크업 스켈레톤 본문이 그쪽이고, 가로 스크롤은 375px에서 Task 025 DoD와
   충돌할 위험이 크다). 022B 구현이 이미 그 값이라 코드 조치는 없고 설계서 정정만 남는다.

## 팀장 전체 테스트 (항상 실행)

- npm run lint: 통과 (에러·경고 0건)
- npm run typecheck: 통과 (`next typegen && tsc --noEmit`)
- npm run test: 통과 (18개 파일 · **192개 케이스** — 12일차와 동일. **이번 회차 산출물이 전부
  화면 컴포넌트라 `lib/` 순수 함수 테스트 대상이 없다**, `docs/CONVENTIONS.md` §9)
- npm run build: 통과. 라우트 **20종**(정적 6 · 동적 14) — 신규 라우트 없음. `/keywords`가
  `ScreenPlaceholder`에서 실제 화면으로 바뀌었다

## 문서 갱신

- docs/ROADMAP/work/*.md 상태 마커: `02.크롤파이프라인.md` **022A** · `01.저장소계층.md` **022B**
- docs/ROADMAP.md: 완료 체크 **022**(두 조각이 모두 끝나 원 Task를 접었다). DoD 6개 체크박스에 실측
  근거 기재. 「결과물」·「남긴 한계」 줄 신규. 진행률 **Phase 5: 4/4(✅ 완료)** · 합계 **22/25 (88%)**.
  Phase 5 제목에 ✅ 부착. **Task 023 블록에 13일차에 늘어난 범위 3건을 명시**(I-018/D-039의 코드 4건 ·
  설계서 03 정정 2건 · DoD 문구 정정)
- docs/ROADMAP/work/*.md 영역 정의: 변경 없음
- docs/screens/*: `verification-checklist.draft.md`·`playwright-scenarios.draft.md` 갱신(설계서 본문은
  손대지 않았다 — 정정은 Task 023 몫이다)
- docs/ISSUES.md: **I-027**(DoD ② 문구) · **I-028**(dev 서버 이중화, 해결) · **I-029**(브라우저 공유) ·
  **I-030**(캐시 원복 누락, 해결) 등재
- docs/DECISIONS.md: **D-040**(Top 5는 필터 후 items 슬라이스) · **D-041**(`?runId`는 선택만, 자동 분석
  안 함) · **D-042**(`force`는 불용어 추가 직후에만) · **D-043**(Top 5 모바일은 2열 줄바꿈) 등재.
  draft 3건 병합 후 삭제

## 다음 회차에 열리는 Task

- **Task 023**(크롤 파이프라인) — 임시 코드 제거 및 설계 문서 정정. 선행 6건(009B·012B·016B·018B·
  022A·022B)이 **이번 회차로 전부 충족**됐다. 범위는 D-039와 이번 회차 등재분으로 이미 확정돼 있다.
- 저장소 계층·화면은 **열린 Task가 없다** — 유휴다. 저장소 계층은 25~28주차 항목(023이 반영할 문서
  정정 초안), 화면은 24~25주차 항목(025 리허설 · 반응형 점검)이 배정 대상이다.
- 그 뒤로는 **024(화면) → 025(화면)** 가 직렬로 남는다. 병렬 폭이 1이라 회차당 하나씩이다.

### 인계 사항

- **023 범위는 늘었다.** 「임시 코드 삭제뿐」이 아니다. `docs/ROADMAP.md` Task 023 블록에 세 묶음을
  명시해 뒀다: ① `app/api/kiwi-check/route.ts`·`components/common/screen-placeholder.tsx` 삭제(원 범위)
  ② `lib/crawler/{run.ts,types.ts,index.ts,press-crawler.ts}`(**I-018**, 범위는 **D-039**가 확정)
  ③ 설계서 03 정정 2건(**I-026** 더미 `RunOption` · **D-043** Top 5 모바일).
- **`docs/ROADMAP.md` DoD ②와 §검증 시나리오 4-3의 "절반 가까이" 문구는 팀장이 고친다**(I-027).
  이 파일은 워크스트림이 편집하지 않는다.
- **`ScreenPlaceholder` 삭제 시 `grep -r "ScreenPlaceholder"`가 0건이어야 한다.** 이번 회차로 마지막
  사용처(`/keywords`)가 사라졌다.
- **Task 025는 브라우저를 단독으로 쓰는 회차에 태운다**(I-029). "끊지 않고 1회 통과"라는 DoD가 공유
  브라우저에서는 성립하지 않는다.
- **`data/runs/`에 실행이 22건 쌓였다.** 025는 `data/`를 비운 초기 상태에서 시작해야 하므로 그 회차
  직전에 정리 시점을 정한다. 12일차부터 이월된 항목이다.
- **검증 후 원복은 "불용어 삭제 + `force=true` 재분석"까지다**(I-030).

## git

- 브랜치: day-13 (day-12에서 분기)
- 커밋: 회차 마감 커밋 1건
- 푸시: 사용자 확인 후 결정
