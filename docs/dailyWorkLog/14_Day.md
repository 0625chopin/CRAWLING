# 14일차 작업 로그 (2026-08-11)

## 회차 요약

- 활성 워크스트림: 크롤 파이프라인 · 저장소 계층(유휴 배정) · 화면(유휴 배정)
- 이번 회차 배치 근거: 13일차에 **022A·022B**가 닫히면서 Task 023의 선행 6건(009B·012B·016B·018B·
  022A·022B)이 **전부 충족**됐다. 저장소 계층과 화면은 Phase 6이 병렬 폭 1의 직렬 꼬리라 열린 Task가 없다.
- 결과: **교차검증 전 항목 pass**, 신규 등재 이슈 2건(I-031·I-032), 전체 테스트 통과
- **Task 023이 닫히면서 진행률이 23/25(92%)가 됐다.** 남은 것은 024·025 둘뿐이다.
- **저장소에서 임시 코드가 사라졌다** — `app/api/kiwi-check/route.ts` · `components/common/screen-placeholder.tsx` ·
  `lib/crawler/run.ts`. 빌드 라우트가 20종에서 **19종**으로 줄었다.

### 이번 회차의 성격 — 유휴 배정이 본 작업의 범위를 두 번 넓혔다

**만드는 회차가 아니라 지우고 맞추는 회차였고, 실제 값의 절반이 유휴 배정에서 나왔다.**
저장소 계층은 담당의 삭제 범위를 **독립적으로 import 그래프까지 재추적**해 D-039가 맞음을 확인하는 동시에
**023이 빠뜨린 두 건**을 찾았다 — `docs/PRD.md` 249·250행의 끊긴 참조와, 가상 수치가 한 곳이 아니라
**세 곳(그중 하나는 TSX 코드 블록)**에 복제돼 있다는 사실이다. 화면은 024 사전 조사에서
**`.env.example`의 변수 누락 1건**을 찾아 15일차가 그대로 집어 들 수 있게 만들었다.

**둘 다 담당 혼자서는 안 나왔을 것이다.** 담당은 ROADMAP이 지시한 범위 안에서 정확히 일했고, 그 범위 밖에
있던 것을 다른 갈래가 찾았다.

## 워크스트림별 완료 내역

### 크롤 파이프라인 (02.크롤파이프라인.md)

- 완료 Task: **023** · 임시 코드 제거 및 설계 문서 정정
- 산출물(삭제): `app/api/kiwi-check/route.ts`(디렉터리째) · `components/common/screen-placeholder.tsx` ·
  `lib/crawler/run.ts`
- 산출물(수정): `lib/crawler/types.ts`(`crawlRequestSchema`·`CrawlRequest`만 제거) ·
  `lib/crawler/index.ts`(재수출 줄) · `lib/crawler/press-crawler.ts`(주석) ·
  `docs/screens/03-hot-keyword.md`(I-026 경고 절 · D-043 · 가상 수치 3곳) · `docs/PRD.md`(249·250행)
- 비고: **삭제가 만든 거짓 서술을 함께 고친 판단이 옳았다.** `types.ts`의 "API 라우트와 러너가 같은
  스키마를 공유한다"는 주석은 러너가 사라진 뒤 거짓이 됐고, 담당은 이를 고친 뒤 **"D-039가 명시한 4개 항목
  밖의 자발적 편집"이라고 스스로 보고했다.** 범위를 넘었다고 밝히는 처신이 정확했고, 팀장 판정은 "유지"다 —
  삭제가 만든 거짓 주석을 고치는 것은 삭제 작업의 일부다. 같은 근거로 `docs/PRD.md` 249·250행도 정정했다.
- 비고: **I-026을 mock 블록 삭제가 아니라 경고 절 삽입으로 처리했다.** 리뷰가
  `docs/screens/02-collect-result.md:350`의 I-024 처리와 대조해 같은 패턴임을 확인했다.

### 저장소 계층 (01.저장소계층.md) — 유휴 배정

- 완료 Task: 없음(열린 Task 없음). **023 삭제 범위 독립 검증 · I-027 문구 대안 · 가상 수치 전수 조사**
- 산출물: 삭제 안전성 검증 결과(존치 6종의 소비자 파일:라인 목록) · I-027 대체 문구 3안 ·
  `03-hot-keyword.md` 가상 수치 3곳 위치 · `docs/PRD.md` 끊긴 참조 2건
- 비고: **"지워도 되는가"보다 "남겨야 할 것이 실수로 지워지지 않았는가"를 먼저 본 것이 주효했다.**
  존치 6종 각각의 소비자를 파일:라인으로 열거해 `fetch-html.ts:3,9,10` · `rss.ts:6,260` ·
  `press-crawler.ts:13`을 확정했다. **`lib/**/*.test.ts`까지 포함해 훑어** 삭제로 `npm run test`가 깨지지
  않음을 미리 확인한 것도 담당이 build를 못 돌리는 상황에서 값이 컸다.
- 비고: **`ResolvedCrawlTarget`·`CrawlSuccess`는 이름으로 직접 import하는 곳이 0건**임을 밝히면서도,
  `fetchHtml`의 반환 타입 `CrawlResult`가 이 둘의 유니온이라 **구조적으로 필요하다**고 판정해 D-039의
  "나머지 유지"가 옳음을 확인했다. 0건이라는 사실만 보고 지우자고 하지 않은 것이 정확하다.

### 화면 (03.화면.md) — 유휴 배정

- 완료 Task: 없음(열린 Task 없음). **023 교차검증 · 024 사전 조사 · 025 절차 정비**
- 산출물: `docs/env-audit.draft.md`(신규 — 환경변수 전수 대조표 · README 정합성 대조) ·
  `docs/screens/verification-checklist.draft.md`(3부 신규 절 — 025 남은 4개 항목 실행 절차 ·
  `data/` 백업·초기화·복원 절차)
- 비고: **`process.env`를 읽는 파일이 `lib/crawler/config.ts` 단 하나임을 전수 조사로 확정**하고, 7개 변수를
  6칸(이름·코드 기본값·`.env.example` 유무·거기 값·README 유무·ROADMAP 유무)으로 대조해
  **`CRAWL_PRESS_CONCURRENCY` 하나만 어긋남**을 찾았다(I-032).
- 비고: README 정합성은 **어긋나는 문장 0건**이었다. "고칠 것이 없다"도 결론이며, 024 블록이 "없으면 변경
  없음으로 끝낸다"고 명시한 그대로다. 다만 **`.env.local`을 실제로 지우고 1회 실행하는 검증은 못 했다** —
  dev 서버를 다른 워크스트림이 쓰고 있었다. 15일차 024가 반드시 수행한다.

## 교차검증 결과

- **화면 → 크롤 파이프라인(023)**: DoD 4개 + D-039 존치 6종 생존 + 죽은 코드 제거 + 설계서 정정 3종 +
  실물 일치 + ROADMAP 미편집까지 **전 항목 pass**. 리뷰가 세 가지를 더 했다:
  - ① **`/press`에서 `[피드 테스트]`를 실제로 눌렀다.** 블로터 RSS로 "기사 50건 · 요약 평균 299자" +
    샘플 3건이 정상 렌더됐다. **이것이 `fetchHtml`·`rss.ts` 경로, 즉 D-039가 살려 둔 타입들이 실제로 살아
    동작하는지를 보는 가장 싼 방법**이고, `types.ts`에서 뭔가 잘못 지워졌다면 여기서 드러났을 것이다.
    라우트 5개가 200이라는 것만으로는 이 확인이 안 된다.
  - ② **I-026 처리 패턴을 `02-collect-result.md:350`과 직접 대조**해 같은 방식임을 확인했다.
    "같은 패턴이다"라는 담당 주장을 문서를 열어 검증한 것이다.
  - ③ **DoD ③을 grep이 아니라 읽어서** 판정했다(65~68·277·759~780행).
- **이번 회차 리뷰도 fail 0이다** — 12·13일차에 이어 세 회차 연속이다. 다만 **본 작업의 범위 누락 두 건은
  리뷰가 아니라 유휴 배정이 잡았다.** 리뷰는 "지시받은 것을 제대로 했는가"를 보고, 유휴 배정은 "지시 자체가
  빠뜨린 것이 있는가"를 봤다. 두 안전망의 성격이 다르다.

## DoD 충족 현황

| Task | DoD 충족 | 미충족 항목 |
| --- | --- | --- |
| 023 | **4/4** | 없음. ②는 **코드 기준 0건**이고 `docs/` 하위 약 40건은 과거 진행 기록·설계 각주다(리뷰 판정) |

- Playwright MCP 동선(023): 5개 라우트(`/` `/press` `/results` `/stopwords` `/keywords`)를 담당과 리뷰가
  **각각 따로** 열어 200·정상 렌더·콘솔 에러 0건 확인. `/`에서 언론사 4곳 체크박스 목록 렌더로 크롤 경로
  생존 확인. **리뷰가 추가로 `[피드 테스트]`를 실제 실행**해 `fetchHtml`·`rss.ts`가 동작함을 확인하고
  `[취소]`로 닫아 데이터 변경 없음.

### 실측 — 삭제 범위 검증

| 심볼 | 판정 | 근거 |
| --- | --- | --- |
| `runCrawl` · `RunCrawlOptions` | **삭제** | 테스트 포함 전 범위 소비자 0건 |
| `crawlRequestSchema` · `CrawlRequest` | **삭제** | `run.ts`조차 참조하지 않았고 `index.ts` 재수출뿐 |
| `crawlTargetSchema` · `CrawlTarget` · `CrawlResult` | **존치** | `fetch-html.ts:3,9,10` |
| `CrawlFailure` | **존치** | `rss.ts:6,260` · `press-crawler.ts:13` 및 실사용 다수 |
| `ResolvedCrawlTarget` · `CrawlSuccess` | **존치** | 직접 import 0건이나 `CrawlResult` 유니온의 구성원이라 구조적으로 필요 |

- **가상 수치 교체**: `docs/screens/03-hot-keyword.md`의 `87건 / 15,204 / 6,318 / 412 / 1,096`을 13일차
  실측값 `53건 / 17,915 / 12,080 / 84 / 2,412`로 세 곳(114행 데스크톱 ASCII · 172~176행 모바일 ASCII ·
  477~482행 `MOCK_SUMMARY` TSX)에서 일제히 교체. 교체 후 그 숫자 세트 grep 0건.
  **`components/keywords/analysis-summary.tsx`가 이 값을 베끼지 않았음**도 확인했다(props로만 받는다).
- **환경변수 전수 조사**: `process.env`를 읽는 파일은 `lib/crawler/config.ts` 하나. 7개 변수 중
  **`CRAWL_PRESS_CONCURRENCY`만 `.env.example`에 없다**(I-032). `lib/keyword/kiwi.ts`는 환경변수를 읽지
  않고 모델 경로가 하드코딩이다(설계 의도).

## 발견·해결한 이슈

1. [저장소 계층 → 신규 등재] **I-031** — `kiwi-check` 삭제로 `userWords` 결합을 태워 확인할 경로가
   사라졌다. **삭제를 막지 않는다** — 지금 `buildKiwi()`가 `userWords: []`라 검증 대상 자체가 없다.
   핵심은 비대칭이다: **Q1이 "자리는 열어 둔다"고 했는데 그 자리를 채운 뒤 결과를 확인할 수단만 없어진다.**
   Q1이 다시 열릴 때 다시 만들어야 할 3단계 진단 경로를 이슈에 적어 뒀다.
2. [화면 → 신규 등재] **I-032** — `CRAWL_PRESS_CONCURRENCY`가 `.env.example`에 없다.
   `readInt`가 조용히 기본값 3으로 폴백해 **화면은 완벽히 멀쩡하다.** 더 나쁜 것은 구조다 —
   README와 ROADMAP이 개별 변수를 나열하지 않고 **"`.env.example`을 보라"고 가리키기만 하므로,
   거기 이름이 없으면 그 가리킴 자체가 이 변수에 대해서만 거짓이 된다.** 세 문서를 아무리 대조해도
   잡히지 않는 종류다. Task 024가 15일차에 반영한다.
3. [저장소 계층이 잡은 범위 누락] **`docs/PRD.md` 249·250행이 삭제된 것을 가리키고 있었다** —
   249행은 `lib/crawler/run.ts`를, 250행은 `crawlRequestSchema`를 참조했다. Task 023의
   「생성/수정 파일」에 `docs/PRD.md`가 없어 누락될 뻔했다. 249행은 `press-crawler.ts`로, 250행은
   `crawlTargetSchema`와 실제 소비자 `fetch-html.ts`로 정정했다.
4. [저장소 계층이 잡은 범위 누락] **가상 수치가 한 곳이 아니라 세 곳이었다** — I-027이 지목한
   데스크톱 와이어프레임 외에 모바일 와이어프레임과 **`MOCK_SUMMARY` TSX 코드 블록**에도 같은 값이
   복제돼 있었다. **세 번째가 코드라 서술만 고치면 그대로 남고, 다음 사람이 스켈레톤을 베끼면 근거 없는
   수치가 다시 퍼진다** — I-026·I-024와 정확히 같은 계열의 함정이다.
5. [팀장 → I-027 해소] **DoD ② 문구를 정정했다.** 저장소 계층이 제안한 3안 중 **2안(비율을 박지 않고
   방향만 요구 + 괄호로 근거 병기)**을 택했다. 특정 비율을 다시 박으면 다른 run에서 또 어긋날 여지가
   있다는 판단이다. `docs/ROADMAP.md` Task 022 DoD ②와 §검증 시나리오 4-3 두 곳을 함께 고쳤다.
6. [팀장 → 정합성] **Phase 0 블록이 존재하지 않는 라우트를 완료 근거로 들고 있었다.**
   "`GET /api/kiwi-check`가 키워드 JSON을 돌려준다"가 완료 기준으로 남아 있었다. 당시 판정 근거는 그대로
   두되 **삭제 사실과 대체 확인 수단(`/keywords` 화면)을 괄호로 덧붙였다.** 과거 기록을 고쳐 쓰지 않는다.

## 팀장 전체 테스트 (항상 실행)

- npm run lint: 통과 (에러·경고 0건)
- npm run typecheck: 통과 (`next typegen && tsc --noEmit`)
- npm run test: 통과 (18개 파일 · **192개 케이스** — 13일차와 동일. 삭제된 코드를 참조하는 테스트가
  없었음을 저장소 계층이 착수 전에 확인해 둔 결과다)
- npm run build: 통과. 라우트 **19종**(정적 6 · 동적 13) — 13일차 20종에서 **`ƒ /api/kiwi-check` 제거**

## 문서 갱신

- docs/ROADMAP/work/*.md 상태 마커: `02.크롤파이프라인.md` **023**
- docs/ROADMAP.md: 완료 체크 **023**. DoD 4개 체크박스에 근거 기재. 「결과물」·「남긴 한계」 줄 신규.
  진행률 **Phase 6: 1/3(🟡 진행 중)** · 합계 **23/25 (92%)**.
  **I-027 문구 정정 2곳**(Task 022 DoD ② · §검증 시나리오 4-3). **Phase 0 블록에 `kiwi-check` 삭제 사실 부기**
- docs/ROADMAP/work/*.md 영역 정의: 변경 없음
- docs/PRD.md: 249·250행 끊긴 참조 정정
- docs/screens/03-hot-keyword.md: I-026 경고 절 신규 · D-043 반영(180~182·205행) · 가상 수치 3곳 교체
- docs/screens/verification-checklist.draft.md: 3부 신규(025 남은 4개 절차 · `data/` 백업·복원 절차)
- docs/env-audit.draft.md: 신규(환경변수 6칸 대조표 · README 정합성 대조)
- docs/ISSUES.md: **I-031**(`userWords` 확인 경로 소실) · **I-032**(`CRAWL_PRESS_CONCURRENCY` 누락) 등재.
  **I-027은 이번 회차에 해소**(문구 정정 완료)
- docs/DECISIONS.md: 변경 없음(이번 회차에 새 결정이 필요한 판단이 없었다 — 범위가 D-039·D-043으로
  이미 확정돼 있었다)

## 다음 회차에 열리는 Task

- **Task 024**(화면) — 실행 안내 최종 점검(환경변수 동기화). `의존: 023` 충족.
  **이번 회차 유휴 배정으로 사전 조사가 끝나 있다** — `.env.example`에 추가할 줄(`CRAWL_PRESS_CONCURRENCY=3`)과
  위치까지 확정됐고(I-032), README는 어긋나는 문장이 0건이다. **다만 `.env.local`을 실제로 지우고 README
  순서를 1회 태우는 검증은 15일차에 반드시 해야 한다** — 정적 대조로는 못 잡는 런타임 문제 확인용이다.
- 크롤 파이프라인·저장소 계층은 **열린 Task가 없다** — 유휴다. `02.크롤파이프라인.md` 26~28주차,
  `01.저장소계층.md` 25~28주차 항목이 배정 대상이며 둘 다 "025에서 나올 회귀 수정 대기"다.
- **Task 025는 024가 끝나야 열린다 — 16일차 몫이다.** 병렬 폭이 1이라 회차당 하나씩이다.

### 인계 사항

- **024는 "문서를 새로 쓰는 일이 아니다."** ROADMAP 블록이 명시한다 — README 전면 재작성 금지.
  Phase 3~5가 실제로 추가한 것만 반영하고, 막히는 지점만 고친다. **지금까지 확인된 유일한 실제 변경은
  `CRAWL_PRESS_CONCURRENCY` 한 줄**이다(I-032).
- **Task 025는 브라우저를 단독으로 쓰는 회차에 태운다**(I-029). "끊지 않고 1회 통과"라는 DoD가 공유
  브라우저에서는 성립하지 않는다. 13·14일차에 세 워크스트림이 모두 간섭을 겪었다.
- **`data/runs/`에 실행이 22건 쌓였다.** 025는 `data/`를 비운 완전 초기 상태에서 시작해야 한다.
  **백업→초기화→수행→복원 절차가 `docs/screens/verification-checklist.draft.md` 3부에 명령까지 적혀 있다**
  (`data-backup/`에 `press-sources.json`·`stopwords.json`·`runs/`만 복사, `kiwi-model`은 제외).
  12일차부터 이월된 항목이고 이제 절차가 준비됐다.
- **025 체크리스트는 62개 항목 중 58개가 실행 가능**하다. 남은 4개(4-7·4-11·R-4·R-5)의 실행 절차도
  3부에 적혀 있다.
- **검증 후 원복은 "불용어 삭제 + `force=true` 재분석"까지다**(I-030).
- **개발 서버는 3100번(`next dev`)이다**(I-028). 3000번을 쓰지 않는다.

## git

- 브랜치: day-14 (day-13에서 분기)
- 커밋: 회차 마감 커밋 1건
- 푸시: 사용자 확인 후 결정
