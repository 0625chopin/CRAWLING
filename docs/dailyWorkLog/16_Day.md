# 16일차 작업 로그 (2026-08-11)

## 회차 요약

- 활성 워크스트림: 화면(담당) · 크롤 파이프라인(회귀 수정) · 저장소 계층(교차검증)
- 이번 회차 배치 근거: 15일차에 Task 024가 닫히면서 `의존: 024`가 충족돼 **마지막 Task 025**가 열렸다.
- 결과: 이슈 5건 등재(I-038~I-042) / **025를 막는 결함 전건 해소**, 전체 테스트 4종 통과
- **이 회차로 25개 Task가 전부 완료됐다. MVP가 닫혔다.**

## 워크스트림별 완료 내역

### 화면 (03.화면.md)

- 완료 Task: **Task 025 · MVP 전체 사용자 여정 수동 검증**
- 산출물: 새로 만든 파일 없음(검증 전용 Task). §검증 시나리오 62개 항목 전수 통과 기록.
- 비고: `data/` 백업 → 초기화 → 시나리오 수행 → 복원 → `diff -rq` 전 절차를 두 번(본 검증·재검증) 밟았다.
  임시 언론사·임시 run·불용어 변경·`keywords.json` 캐시까지 원상 복구했다.

### 크롤 파이프라인 (02.크롤파이프라인.md)

- 완료 Task: 없음(유휴 배정 "025에서 나올 회귀 수정 대기"가 실제로 발동했다)
- 산출물: `components/crawl/crawl-run-panel.tsx`(I-038 수정) · `docs/screens/01-crawl-run.md`(I-041 수정)
- 비고: Task 016B를 다시 열어 고쳤다. 같은 종류의 결함(사용자 노출 문자열에 개발 메모가 섞인 것)이 이 영역
  산출물 전체에 더 있는지 훑었고 이 한 곳뿐이었다.

### 저장소 계층 (01.저장소계층.md)

- 완료 Task: 없음(리뷰 상대로서 025 교차검증)
- 산출물: 없음(검증 보고)
- 비고: lint·typecheck·test·build를 **독립 재실행**했고, `data/` 복원을 파일로 직접 확인했다.

## 교차검증 결과

- 저장소 계층 → 화면(Task 025): **pass.** 독립 재현 가능한 모든 것 — 테스트 4종, `data/` 파일 상태,
  `keywords.json` 캐시 값, diff 2건, 5-3 대조 — 이 화면 보고와 전부 일치했고 모순 지점이 0건이었다.
  DoD ①의 범위 표기가 실제 시나리오보다 좁다는 문서 결함을 새로 잡아냈다(**I-042**).
- 화면 → 크롤 파이프라인(016B 회귀 수정): **pass.** `browser_evaluate`로 Alert DOM을 직접 읽어
  `description: "재검증실패언론사(셀렉터 불일치)"`만 나오고 `innerHTML`에 숨은 텍스트 노드도 없음을 확인했다.
  수정된 설계서 예시 문자열과 실물이 같은 형태임도 대조했다.

## DoD 충족 현황

| Task | DoD 충족 | 미충족 항목 |
| --- | --- | --- |
| 025 | 5/5 | 없음 |

- Playwright MCP 동선: `data/`를 비운 초기 상태에서 빈 상태 5종 확인 → `/press`에서 RSS 요약(블로터)·본문 전문
  수집 ON·HTML 목록(ZDNet) 3종 등록 → `/`에서 3곳 선택 후 크롤 실행(1초 폴링 갱신 관찰) → `/results`에서 파일
  미리보기·본문 출처 배지·검색 → `[키워드 분석]` → 분석 실행 후 조사 제거·함정 ②·1글자 필터·최소 횟수·품사
  필터 확인 → 랭킹에서 불용어 추가 → `/stopwords` 일괄 추가·중복 거부·프리셋 삭제 다이얼로그 → 재분석 →
  언론사 비활성 토글 후 재크롤 → 언론사 삭제 후 과거 run 조회. 375/768/1280px 세 폭과 다크모드, 키보드 단독
  조작까지 각각 수행했다.

## 발견·해결한 이슈

1. [화면 → 크롤 파이프라인] **I-038** — 부분 실패 Alert에 개발자 메모(`— 수집 방식에 따라 사유 문구가 다르다.`)가
   사용자 노출 텍스트로 섞여 나갔다 → 016B를 다시 열어 제거하고 그 사실을 코드 주석으로 옮겼다
   (재검증 화면 pass). **`run-meta.json`에는 드러나지 않고 화면을 띄워야만 보이는 종류였다.**
2. [크롤 파이프라인] **I-041** — 그 결함의 오염원이 `docs/screens/01-crawl-run.md`의 마크업 스켈레톤 예시
   문자열이었다 → 설계서 쪽도 고쳐 재발을 막았다. **코드만 고치면 다음 구현자가 같은 문자열을 또 베낀다.**
3. [화면 → 팀장] **I-039** — §검증 시나리오 5-3의 예시 입력 `앵커`가 기본 불용어 7건과 겹쳐, 정상 동작이
   기대 결과와 어긋나 보였다 → 팀장이 ROADMAP의 예시 단어를 교체했다.
4. [저장소 계층 → 팀장] **I-042** — Task 025 DoD ①이 "1~5단계 + 분기 A·B"라 실제 시나리오(0~6단계 +
   반응형·접근성)보다 좁았다 → 팀장이 실제 범위로 정정했다. **완료 판정 기준이 검증 범위보다 좁은 것 자체가
   위험하다.**
5. [크롤 파이프라인] **I-040** — 부분 실패 Alert의 폴백 문구가 기사 단위 실패에서도 뜬다 → **MVP가 안고 가는
   알려진 한계로 남겼다.** 집계 단위를 맞추려면 007·014A·016B의 계약을 동시에 건드려야 한다.

## 팀장 전체 테스트 (항상 실행)

- npm run lint: 통과 (경고 0)
- npm run typecheck: 통과 (`next typegen` → 라우트 타입 생성 성공, `tsc --noEmit` 오류 0)
- npm run test: 통과 (vitest 18파일 / 192케이스 전부 통과)
- npm run build: 통과 (19개 라우트 — static 6, dynamic 13)

저장소 계층이 교차검증에서 같은 4종을 독립적으로 재실행해 동일 결과를 얻었다.

## 문서 갱신

- docs/ROADMAP/work/*.md 상태 마커: 03.화면.md Task 025 → `상태: 완료 (16일차, 2026-08-11)`
- docs/ROADMAP.md: 완료 체크 **Task 025** · 진행률 **Phase 6: 3/3 ✅, 합계 25/25 (100%)** ·
  Phase 6 제목에 ✅ · Task 025 DoD ① 범위 정정(I-042) · §검증 시나리오 5-3 예시 입력 교체(I-039)
- docs/ISSUES.md: **I-038 ~ I-042** 병합 등재. draft 2건(화면·크롤파이프라인) 삭제.
  I-011 블록에 16일차 실크롤 재관찰 메모 추가(새 번호 없이 기존 블록 내 추가).
- docs/DECISIONS.md: **D-044**(025 재실행 범위는 "번호 붙은 스텝이 막혔는가"로 가른다) 등재.
- docs/ROADMAP/work/*.md 영역 정의: 변경 없음

## 이 회차에서 배운 것

**검증 문서 자체가 검증 대상이다.** 이번에 잡힌 5건 중 2건(I-039·I-042)이 코드가 아니라 **검증 문서의 결함**이었다.
기대 결과가 실제와 어긋나면 다음 사람이 정상 동작을 결함으로 신고하거나 반대로 결함을 정상으로 넘긴다. DoD가
실제 범위보다 좁으면 절반만 태우고도 완료로 볼 수 있다. **25개 Task를 통과시키고 마지막에 그 두 개가 나왔다는
것은, 문서를 실제로 태워 보기 전에는 아무도 그것이 틀렸는지 모른다는 뜻이다.**

**판정 방법이 결과를 갈랐다.** I-038은 접근성 트리 스냅샷으로는 눈에 덜 띄었고 `browser_evaluate`로
`innerHTML`을 직접 읽어야 드러났다. 4-11(Kiwi 싱글턴)은 캐시가 있으면 Kiwi를 아예 타지 않아 응답 시간만 보면
"빠르네" 하고 잘못 닫힐 뻔했고, `force=true` 경로로 진짜 콜드 호출을 만들어서야 1675ms → 26ms라는 근거가 나왔다.
**무엇을 봤는가보다 어떻게 봤는가가 판정을 좌우한다.**

## 다음 회차에 열리는 Task

**없다. 25개 Task가 모두 완료됐다.** `docs/ROADMAP.md`의 Task 목록에 미완료 항목이 남아 있지 않으므로 다음
회차에 산정할 배치가 존재하지 않는다. 새 스코프를 로드맵에 추가하지 않는 한 17일차는 열리지 않는다.

MVP 이후를 연다면 이미 문서에 근거가 쌓여 있는 것부터다 — **I-040**(부분 실패 집계 단위), **I-011**(HTML 엔티티
디코딩), ROADMAP §결정 필요 사항의 **Q1**(`userWords` 사용자 사전)과 **Q6**(RSS `pubDate` 기간 필터). 셋 다
MVP 범위 밖으로 뺀 근거와 도입 시 전제 조건이 함께 적혀 있다.

## git

- 브랜치: day-16
- 커밋: 16일차 — MVP 전체 사용자 여정 수동 검증(025)으로 25개 Task 전부 완료
- 푸시: 사용자 승인 대기
