# 검증 체크리스트 초안

**작성 배경** — 이번 회차(화면 워크스트림 유휴 대체작업)는 `docs/ROADMAP/work/03.화면.md`의 「24~25주차 유휴에 맡길 것」 항목인
**Task 025(MVP 전체 사용자 여정 수동 검증) 사전 리허설**과 **375/768/1280 반응형 점검 항목 정리**를 담는다. 코드는 건드리지
않았고, 아래는 실제로 Task 025 및 각 화면 Task 완료 회차에 그대로 태울 수 있는 실행 가능한 체크 항목이다.

**작성 시점** — 6일차(2026-08-10) 완료 기준. 아래 "0. 현재 구현 상태 스냅샷"이 이 시점의 실물 코드를 근거로 한다 — 추측이 아니라
`app/*/page.tsx`를 직접 열어 확인했다.

---

## 0. 현재 구현 상태 스냅샷 (6일차, 2026-08-10 기준)

| 화면 | 라우트 | 실제 상태 | 근거 |
| --- | --- | --- | --- |
| 크롤링 실행 (홈) | `/` | `ScreenPlaceholder`만 렌더링 | `app/page.tsx` — Task 016A/016B 대기 |
| 수집 결과 | `/results` | `ScreenPlaceholder`만 렌더링 | `app/results/page.tsx` — Task 018A/018B 대기 |
| 핫 키워드 분석 | `/keywords` | `ScreenPlaceholder`만 렌더링 | `app/keywords/page.tsx` — Task 021·022A/022B 대기 |
| 언론사 관리 | `/press` | **실제 화면** (목록·추가·수정·삭제·활성 토글 전부 동작) | `app/press/page.tsx` — Task 009A·009B 완료 |
| 불용어 관리 | `/stopwords` | **실제 화면** (섹션·칩·추가·일괄추가·검색·삭제 전부 동작) | `app/stopwords/page.tsx` — Task 012A·012B 완료 |

**API·라이브러리 계층 보조 확인**(화면 실행 가능 여부에 직결):

| 계층 | 상태 | 화면 검증에 미치는 영향 |
| --- | --- | --- |
| 언론사 CRUD API(008A/008B) | 완료 | `/press` 전체 동선 실행 가능 |
| 불용어 CRUD API(011) | 완료 | `/stopwords` 전체 동선 실행 가능 |
| 소스 테스트 API(010B, `test-source` 라우트·`source-test-panel.tsx`) | **대기** | `/press` 폼의 `[피드 테스트]`·`[셀렉터 테스트]` 버튼이 아직 없다 |
| 크롤 오케스트레이터(013A/013B, `lib/crawler/press-crawler.ts`) | 완료 | 언론사 단위 수집 로직 자체는 있지만, 화면·API로 트리거할 방법이 아직 없다 |
| 실행 잡 관리자·진행 상태(014·015) | **대기** | 실제 크롤을 시작하고 진행률을 볼 방법이 없다 — `data/runs/`가 만들어지지 않는다 |
| 수집 결과 API(017) | **대기** | `/results`가 참조할 run 데이터를 API로 조회할 수 없다 |
| 키워드 추출·집계(020, `lib/keyword/`) | 완료 | 라이브러리는 준비됐지만 API(021)·화면(022)이 없어 브라우저에서 실행할 수 없다 |
| 키워드 분석 API·캐시(021) | **대기** | |

**결론**: 이번 회차에 브라우저로 지금 태울 수 있는 것은 **언론사 관리·불용어 관리 두 화면과 그 안에서 완결되는 동선뿐**이다.
크롤링 실행 이후의 모든 단계(진행 관찰 → 결과 확인 → 키워드 분석 → 재분석 → 재크롤링)는 최소 Task 014·015·016·017·018·021·022가
끝나야 실행 가능하다.

---

# 1부 — Task 025 사전 리허설: 0~6단계 + 반응형·접근성 최종 확인

원본은 `docs/ROADMAP.md` §검증 시나리오(0~6단계, 반응형·접근성 최종 확인)이고 여정 근거는 `docs/PRD.md` §사용자 여정 1→5다.
아래 표의 "지금" 열은 이번 회차(6일차) 기준이며, Task 025가 실제로 실행되는 회차에는 그 시점의 완료 Task로 다시 갱신해야 한다 —
이 표는 고정된 결과가 아니라 **매 회차 재평가할 틀**이다.

## 사전 준비 (모든 단계 공통)

```bash
rm -rf data/runs data/press-sources.json data/stopwords.json   # kiwi-model은 남긴다
npm run dev
```

- [ ] 위 명령으로 `data/`를 완전 초기 상태로 만든 뒤 서버를 띄운다.
- [ ] Playwright MCP로 데스크톱(1280×800)과 모바일(375×812) 두 벌을 각각 수행한다(Task 025 구현 규칙).

## 0단계 — 초기 빈 상태 확인

| # | 행동(무엇을 누르는가) | 확인(무엇을 보는가) | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| 0-1 | `browser_navigate` → `/` | "등록된 언론사가 없습니다" 빈 상태 + `[언론사 관리로 이동]` 버튼 + `[크롤링 시작]` 버튼이 `disabled` | 대기 | 016A |
| 0-2 | `browser_navigate` → `/results` | "아직 크롤링한 결과가 없습니다" + `[크롤링 실행하러 가기]` | 대기 | 018A |
| 0-3 | `browser_navigate` → `/keywords` | "수집된 데이터가 없습니다" + `[크롤링 실행하러 가기]` | 대기 | 022A |
| 0-4 | `browser_navigate` → `/press` | `Newspaper` 아이콘 빈 상태 + "등록된 언론사가 없습니다" + `[+ 언론사 추가]` | **지금** | — (완료) |
| 0-5 | `browser_navigate` → `/stopwords` | `browser_snapshot`으로 "기본 제공 불용어 (7)" / "사용자 추가 불용어 (0)" 헤딩 확인, 칩 7개가 기자·사진·제공·앵커·무단전재·재배포금지·이번과 정확히 일치 | **지금** | — (완료) |

## 1단계 — 언론사 등록 (`F007`, 여정 [분기 B])

**세 경로를 모두 만든다** — ① RSS(요약만) ② RSS(본문 전문) ③ HTML.

| # | 행동 | 확인 | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| 1-1 | `/press` → `button "언론사 추가"` 클릭 | `dialog "언론사 추가"` 열림, `radio "RSS 피드"`가 기본 checked(수집 방식 토글이 첫 필드) | **지금** | — |
| 1-2 | ① 이름 + 피드 URL만 입력 | 셀렉터 입력란이 하나도 안 보임 | **지금** | — |
| 1-3 | `button "피드 테스트"` 클릭 | "기사 N건 · 요약 평균 M자" + 최신 3건 제목, EUC-KR 피드도 한글이 안 깨짐 | 대기 | 010B |
| 1-4 | 피드 URL을 스킴 없이(`etnews.com/rss`) 넣고 저장 시도 | 필드 하단 한국어 오류 문구 + `aria-invalid="true"` | **지금** | — |
| 1-5 | 정상 입력 후 `button "저장"` | 토스트 확인, 목록에 `RSS` 배지 행 추가, `data/press-sources.json` 파일 diff로 실제 기록 확인 | **지금** | — |
| 1-6 | ② 두 번째 RSS 언론사 — "본문 전문 수집" 스위치 ON + 본문 셀렉터 입력 | 스위치 켜면 셀렉터 입력이 펼쳐지고, 비운 채 저장 시도 시 저장이 막힘 | **지금** | — |
| 1-7 | ③ 세 번째 언론사 — 수집 방식 토글을 "목록 페이지"로 전환 | 피드 URL 필드가 사라지고 목록 URL + 셀렉터 3종으로 교체, 포커스가 새 블록 첫 입력으로 이동 | **지금** | — |
| 1-8 | 기사 링크 셀렉터 옆 `button "셀렉터 테스트"` 클릭 | "링크 N개 발견" + 샘플 URL 3건, 틀린 셀렉터면 "0개 발견" destructive Alert | 대기 | 010B |
| 1-9 | 저장 후 `/press` 목록의 `수집 설정` 컬럼 확인 | ①은 `피드 요약만`(텍스트, 버튼 아님) · ②는 `본문 셀렉터 ▸`(버튼) · ③은 `셀렉터 3개 ▸`(버튼) | **지금** | — |
| 1-10 | ②·③ 행의 펼침 버튼 클릭 | 같은 표 안 서브 행(`colSpan`)에 셀렉터가 `font-mono`로 펼쳐짐, `aria-expanded` 상태와 일치 | **지금** | — |
| 1-11 | ③을 수정 다이얼로그에서 다시 RSS로 바꿔 저장 → 다시 열기 | 저장된 JSON에 `listUrl`·`articleLinkSelector`·`titleSelector`가 남아 있지 않음. **확인 후 원래 방식(HTML)으로 되돌려 둔다** | **지금** | — |

**1단계 요약**: 11개 중 9개 지금 실행 가능, 2개(1-3, 1-8 — 피드/셀렉터 테스트 버튼)는 Task 010B 완료 후로 미룬다.

## 2단계 — 크롤링 실행 및 진행 관찰 (`F001` `F002` `F003`, 여정 1→2)

| # | 행동 | 확인 | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| 2-1 | `browser_navigate` → `/` | 1단계에서 등록한 활성 언론사 3곳이 체크박스로 보이고 각 항목에 `RSS`/`HTML` 배지 + `sourceUrl` 노출 | 대기 | 016A |
| 2-2 | 1곳만 체크 | "1/3개 선택됨" 텍스트, 전체 선택 체크박스가 `aria-checked="mixed"`, `[크롤링 시작]` 활성화 | 대기 | 016A |
| 2-3 | "언론사당 최대 기사 수" = 5 입력 | 입력값 반영 | 대기 | 016A |
| 2-4 | `button "크롤링 시작"` 클릭 | 버튼이 `[중단]`으로 전환, 진행률 텍스트("현재: {언론사} — n/5건")·언론사별 상태 리스트가 1초 간격으로 갱신 | 대기 | 014, 015B, 016B |
| 2-5 | 진행 중 옵션 입력·체크박스 재조작 시도 | 모두 `disabled` | 대기 | 016B |
| 2-6 | 완료 대기 | 완료 요약(언론사 수·기사 수·저장 경로) + 토스트, `[수집 결과 보기]` 노출 | 대기 | 014, 016B |
| 2-7 | `data/runs/{runId}/` 확인 | `run-meta.json` + `articles/0001.txt` 등 존재, 메타 라인 7줄(`contentSource` 포함) | 대기 | 013(완료) + 014·015 |
| 2-8 | txt 메타의 `contentSource` 값 확인 | ①은 `rss-summary`, ②·③은 `article-page` | 대기 | 013(완료) + 014·015 |
| 2-9 | ①과 ③ 본문 길이 비교 | ①(요약)이 ③(전문)보다 눈에 띄게 짧음 | 대기 | 013(완료) + 014·015 |

**2단계 요약**: 9개 전부 대기. Task 013(오케스트레이터)은 6일차에 완료됐지만, 화면(016)·API/진행상태(015B)·잡 관리(014) 없이는
브라우저로 트리거할 방법이 없다 — 부분 실패 상태(셀렉터를 일부러 틀리게 두거나 존재하지 않는 피드 URL) 확인도 같은 이유로 대기.

## 3단계 — 수집 결과 확인 (`F003` `F004`, 여정 3)

| # | 행동 | 확인 | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| 3-1 | `button "수집 결과 보기"` 클릭 | `/results`로 이동, 방금 실행이 셀렉터에 기본 선택 | 대기 | 016B, 017, 018A |
| 3-2 | 실행 요약 카드 확인 | 시각·소요시간·대상 언론사 배지·성공/실패 건수·저장 경로 | 대기 | 017, 018A |
| 3-3 | 파일 미선택 상태 확인 | "파일을 선택하면 본문을 미리 볼 수 있습니다" | 대기 | 018A, 018B |
| 3-4 | 파일 1건 클릭 | 제목·언론사·본문 출처 배지(`원문 전문`/`피드 요약`)·수집 시각 + `whitespace-pre-wrap` 본문, 선택 행 `bg-muted` | 대기 | 017, 018B |
| 3-4b | RSS 요약 기사·전문 기사를 번갈아 선택 | 배지가 각각 `피드 요약`/`원문 전문`으로 전환 | 대기 | 017, 018B |
| 3-5 | 검색창에 제목 일부 입력 | 목록 필터링 | 대기 | 018B |
| 3-6 | 원문 링크(🔗) 클릭 | 새 탭에서 원문 기사가 열림(`target="_blank" rel="noopener noreferrer"`) | 대기 | 018B |
| 3-7 | `button "키워드 분석"` 클릭 | `/keywords?runId={runId}`로 이동 | 대기 | 018A |

**3단계 요약**: 8개 전부 대기 — 2단계에서 만든 run 데이터가 선행 조건이라 결과 화면(017·018)뿐 아니라 크롤 파이프라인 완료도 함께 필요하다.

## 4단계 — 핫 키워드 분석 (`F005` `F006`, 여정 4)

| # | 행동 | 확인 | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| 4-1 | `/keywords` 진입 | run 셀렉터에 3단계에서 넘어온 run이 선택돼 있음 | 대기 | 021, 022A |
| 4-2 | `button "분석 시작"` 클릭 | 3단계 스텝(모델 로딩 → 토큰화 → 집계) 순차 표시, 첫 실행은 1.4초 모델 로딩 포함 | 대기 | 021, 022A |
| 4-3 | 분석 요약 5개 수치 확인 | "조사·어미 제거 후"가 "전체 토큰 수"보다 절반 가까이 작음 | 대기 | 020(완료) + 021, 022A |
| 4-4 | Top 5 카드 확인 | 1위가 가장 큰 폰트 + 가장 진한 상단 강조선, 품사 배지 | 대기 | 022A |
| 4-5 | 랭킹 표 확인 | 빈도 내림차순, 비중 막대 + 백분율 텍스트 병기 | 대기 | 022B |
| 4-6 | 조사 제거 확인 | `삼성전자가`·`삼성전자를` 변형이 `삼성전자` 하나로 합산 | 대기 | 020(완료) + 022B |
| 4-7 | **함정 ② 확인**(`Match.joinAffix` 미사용 검증) | "공개했다"·"적용했다"·"경쟁한다"가 있으면 `공개`·`적용`·`경쟁`이 키워드에 살아 있음 | 대기 | 019(완료, `MATCH_OPTIONS` 고정) + 022B |
| 4-8 | 1글자 필터 확인 | `것`·`수`·`점` 같은 1글자 명사가 랭킹에 없음 | 대기 | 020(완료) + 022B |
| 4-9 | 최소 등장 횟수 3으로 변경 | Kiwi 재실행 없이 즉시 목록이 줄어듦 | 대기 | 021, 022A |
| 4-10 | 품사를 `NNP`만 선택 | 고유명사만 남음 | 대기 | 022A |
| 4-11 | 2회차 분석 실행 | 모델 로딩 단계가 눈에 띄게 빠름(싱글턴 확인) | 대기 | 019(완료) + 021 |

**4단계 요약**: 11개 전부 대기 — 기반 로직(019 Kiwi 어댑터, 020 추출·집계)은 끝났지만 API(021)·화면(022A/022B)이 없어
브라우저에서 확인할 수 없다.

## 5단계 — 불용어로 랭킹 다듬기 (`F008`, 여정 4 [분기 A])

| # | 행동 | 확인 | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| 5-1 | 랭킹 행의 🚫(불용어 추가) 클릭 | 불용어에 추가되고 재분석 후 랭킹에서 사라짐 | 대기 | 022B |
| 5-2 | `button "불용어 관리로 이동"` 클릭 | `/stopwords`로 이동, 방금 추가한 단어가 "사용자 추가" 섹션에 있음 | 대기 | 022B(이동 트리거) — 도착지 화면(012)은 완료 |
| 5-3 | 일괄 추가 패널 펼쳐 `앵커, 특파원\n인턴기자` 입력 | 3건 분리 추가 | **지금**(`/stopwords` 단독 진입 시) | — (완료, 012B) |
| 5-4 | 이미 있는 단어 추가 시도 | destructive Alert "이미 등록된 불용어입니다", 목록 불변 | **지금**(`/stopwords` 단독 진입 시) | — (완료, 012B) |
| 5-5 | 기본 프리셋 칩 ✕ 클릭 | 확인 다이얼로그 노출, 초기 포커스가 "취소" | **지금**(`/stopwords` 단독 진입 시) | — (완료, 012A) |
| 5-6 | `button "분석 페이지로 돌아가 재분석"` 클릭 | `/keywords`로 이동 | 대기(도착 화면 확인 불가) | 022A |
| 5-7 | `button "재분석"` 클릭 | 추가한 불용어들이 모두 랭킹에서 제외됨 | 대기 | 021, 022B |

**5단계 요약**: 여정 순서(랭킹 → 불용어 → 재분석) 전체는 022A/022B가 끝나야 1회로 이어 태울 수 있다. 다만 5-3~5-5는
`/stopwords`를 **단독으로 진입**하면 지금도 개별 확인 가능하다 — Task 025 본번 실행(끊지 않고 1회 통과)에는 포함할 수 없지만,
불용어 화면 자체의 회귀 확인용으로는 지금 활용할 수 있다.

## 6단계 — 재크롤링 루프 (여정 5, [분기 B])

| # | 행동 | 확인 | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| 6-1 | `/press`에서 언론사 1곳을 비활성으로 토글 | 토스트 확인 | **지금**(토글 자체는) | — (완료, 009B) |
| 6-2 | `/`로 이동 | 비활성 언론사가 체크박스 목록에서 사라짐 | 대기 | 016A/016B(009B DoD 이월 항목과 동일 지점) |
| 6-3 | 남은 언론사로 다시 크롤링 | 새 `runId` 폴더 생성 | 대기 | 014, 015, 016 |
| 6-4 | `/results` 실행 셀렉터 확인 | 실행 2건이 최신순으로 보임 | 대기 | 017, 018A |
| 6-5 | 언론사 삭제 후 과거 run 조회 | 500 에러 없이 정상 표시(삭제된 언론사 표시 포함) | 대기 | 017, 018A |

**6단계 요약**: 5개 중 6-1의 "토글 자체"만 지금 가능하고, 그 결과를 확인하는 6-2부터는 홈 화면이 필요해 전부 대기.
이는 `docs/ROADMAP/work/03.화면.md`가 이미 명시한 "009B DoD의 활성 토글 → 홈 반영 재확인은 016B 완료 회차"와 같은 지점이다.

## 반응형 · 접근성 최종 확인 (R-1~R-6)

| # | 확인 | 기대 결과 | 지금 | 선행 Task |
| --- | --- | --- | --- | --- |
| R-1 | 375px 폭에서 5개 화면 순회 | 헤더가 햄버거+Sheet로, 표가 카드 리스트로 전환, 가로 스크롤 없음 | 부분(2/5 화면만) | 016A, 018A, 022A |
| R-2 | 768px 폭에서 5개 화면 순회 | 헤더 메뉴 펼침, 언론사 관리 표 노출 | 부분(2/5 화면만) | 016A, 018A, 022A |
| R-3 | 1280px 폭에서 5개 화면 순회 | 홈·수집 결과의 2단 레이아웃, 우측 패널 sticky 동작 | 대기(홈·결과 자체가 없음) | 016A, 018A |
| R-4 | 키보드만으로 1~5단계 재수행 | 모든 조작 가능, 포커스 링 항상 보임 | 부분(1단계의 언론사 등록 동선만) | 016A, 018A, 022A |
| R-5 | 다크모드 토글 후 5개 화면 재확인 | 하드코딩 색상 없이 정상 전환 | 부분(2/5 화면만) | 016A, 018A, 022A |
| R-6 | 브라우저 콘솔 확인 | 에러·경고 0건 | 부분(2/5 화면만) | 016A, 018A, 022A |

**R단계 요약**: "5개 화면 순회"가 조건이라 Task 025 본번 실행 시점(모든 선행 Task 완료 후)에만 완전히 통과 판정할 수 있다.
지금은 `/press`·`/stopwords` 두 화면만 R-1·R-2·R-4·R-5·R-6의 부분 사전 점검이 가능하다(아래 2부의 화면별 표가 그 상세 항목이다).

---

## 1부 집계

| 구간 | 총 항목 | 지금 실행 가능 | 대기 |
| --- | --- | --- | --- |
| 0단계 | 5 | 2 | 3 |
| 1단계 | 11 | 9 | 2 |
| 2단계 | 9 | 0 | 9 |
| 3단계 | 8 | 0 | 8 |
| 4단계 | 11 | 0 | 11 |
| 5단계 | 7 | 3(부분 진입) | 4 |
| 6단계 | 5 | 1(부분) | 4 |
| R (반응형·접근성) | 6 | 0(전체 통과 기준), 부분 사전점검 5건 | 6 |
| **합계** | **62** | **15**(그중 완결 실행 11 + 부분/사전점검 4) | **47** |

**해석**: 62개 체크 항목 중 지금 "여정을 끊지 않고" 완결 실행할 수 있는 것은 0단계 2개 + 1단계 9개 = **11개**뿐이다(전부 언론사
관리 화면 안에서 끝난다). 나머지는 최소 Task 014·015·016(홈·크롤 실행) 완료가 있어야 2단계부터 열리고, 017·018(결과)·021·022(분석)까지
끝나야 3~6단계와 반응형 최종 확인이 전부 열린다. Task 025는 이 순서대로 다시 회차 배치돼야 한다 — **선행 Task가 하나라도 남아 있으면
"끊지 않고 1회 통과"라는 Task 025 DoD 자체를 시도할 수 없다.**

---

# 2부 — 375 / 768 / 1280 반응형 점검 항목

설계서(`docs/screens/00~05`)에 **명시된 것만** 적었다. 화면별로 전환 기준 폭이 다르므로(04는 `md`=768px, 03은 `sm`=640px과
`lg`=1024px, 01·02는 `lg`=1024px) 다른 화면의 관례를 가져다 쓰지 않는다.

## 공통 (00-app-shell — 5개 화면 전체에 적용)

| 확인 지점 | 375px | 768px | 1280px | 근거 |
| --- | --- | --- | --- | --- |
| 헤더 내비 | 햄버거 트리거만 노출(`md:hidden`), 데스크톱 내비 숨김 | **`md`(768px) 경계** — 데스크톱 내비(`hidden md:flex`) 펼침, 햄버거 사라짐 | 데스크톱 내비 유지 | `00-app-shell.md:120,530,596` |
| 로고 라벨 | 축약형("핫키워드 크롤러") | 축약형 유지(`lg` 미만이라 768~1023px 구간도 축약형) | **`lg`(1024px) 경계** — 전문("IT/AI 뉴스 핫 키워드 크롤러")으로 교체 | `00-app-shell.md:120,703-704` |
| 모바일 Sheet 패널 | `w-72 sm:max-w-sm` 폭, 닫기 버튼 `absolute top-3 right-3`와 첫 메뉴 항목이 `pt-12` 여백으로 안 겹침 | 해당 없음(내비가 이미 펼쳐짐) | 해당 없음 | `00-app-shell.md:153,167,608` |
| 컨테이너 상하 여백 | `py-6` | **`md`(768px) 경계** — `py-8`로 확대 | `py-8` 유지 | `screens/README.md:120-122,242` |

## 04 언론사 관리 (`/press`, Task 009 완료 — 지금 검증 가능)

| 확인 지점 | 375px | 768px | 1280px | 근거 |
| --- | --- | --- | --- | --- |
| 목록 형태 | `PressCardList`(카드, `grid gap-3 md:hidden`) | **`md`(768px) 경계값 — 정확히 768px에서 표로 전환.** 767px에서는 여전히 카드인지 별도 확인 | `PressTable`(`hidden md:block overflow-x-auto`) | `04-press-manage.md:188,193,1021,1156` |
| 카드 내부 구성(375px 전용) | 이름+방식 배지 / URL / 수집 설정 요약 / 활성 스위치(우상단) / 수정·삭제 버튼 모두 세로 배치 | — | — | `04-press-manage.md:193` |
| 수집 설정 서브 행(펼침) | 카드 안 요약 텍스트만(펼침 버튼 없음, 카드 리스트는 표의 서브 행 구조를 쓰지 않는다) | 표 서브 행(`colSpan`)으로 `dl` `sm:grid-cols-3`/`sm:grid-cols-2` | 동일(표 폭 여유로 `sm:` 그리드가 이미 다열) | `04-press-manage.md:1119,1134` |
| 추가/수정 Dialog 폭 | 뷰포트에 맞춰 축소되되 `sm:max-w-lg`로 640px부터 고정폭 | `sm:max-w-lg` 고정 | `sm:max-w-lg` 고정 | `04-press-manage.md:41,705` |
| 페이지 헤더 액션 버튼 | `[+ 언론사 추가]`가 제목과 같은 줄에 들어감(`w-full` 안 줌) | 동일 | 동일 | `04-press-manage.md:186` |

## 05 불용어 관리 (`/stopwords`, Task 012 완료 — 지금 검증 가능)

| 확인 지점 | 375px | 768px | 1280px | 근거 |
| --- | --- | --- | --- | --- |
| 컨테이너 폭 | `max-w-3xl`(README 예외, `width="narrow"`) — 세 폭 모두 **좁은 폭 고정**, 넓어지지 않음 | 동일(좌우 여백만 커짐) | 동일(좌우 여백만 커짐) | `05-stopword-manage.md:25,34,152` |
| 불용어 추가 카드 | `flex flex-col` — 입력이 위, 버튼이 아래로 스택 | **`sm`(640px) 경계** — `sm:flex-row sm:items-end`로 가로 배치 전환 | 가로 배치 유지 | `05-stopword-manage.md:154,467` |
| 헤더 액션 라벨 | "재분석"만 노출(`hidden sm:inline`으로 앞부분 숨김, 제목과 한 줄 유지) | **`sm`(640px) 경계** — "분석 페이지로 돌아가 재분석" 전체 노출 | 전체 노출 유지 | `05-stopword-manage.md:453` |
| 칩 목록 줄바꿈 | `flex-wrap`으로 줄바꿈, 가로 스크롤 없음(`document.body.scrollWidth <= window.innerWidth`로 확인) | 동일(칩 개수가 늘어나도 줄바꿈으로 해결) | 동일 | `playwright-scenarios.draft.md:187-188` |

## 01 크롤링 실행 (홈, `/`, Task 016A/016B 대기 — 화면 완성 후 검증)

| 확인 지점 | 375px | 768px | 1280px | 근거 |
| --- | --- | --- | --- | --- |
| 전체 레이아웃 | 1단 스택(목록 → 실행 패널) | **`lg`(1024px) 미만이면 계속 1단** — `md`(768px)가 아니라 `lg` 기준이므로 768px에서도 아직 스택 유지인지 확인 | **`lg`(1024px) 경계** — `lg:grid-cols-[1fr_360px]` 2단, 우측 `lg:sticky lg:top-20` | `01-crawl-run.md:28,84,421` |
| 언론사 목록 ScrollArea 높이 | 스펙 프로즈(§스크롤 처리 결정, 표 140행)는 `h-[320px]`를 요구 — **단, 스켈레톤 코드는 다르게 돼 있다(아래 이슈 참고), 구현 시 프로즈 값을 따른다** | `h-[420px]` | `h-[420px]` | `01-crawl-run.md:37,140` (스켈레톤 불일치는 draft 이슈 참고) |
| 진행 상태 리스트 ScrollArea | `h-[240px]` 고정(반응형 변화 없음) | 동일 | 동일 | `01-crawl-run.md:39,147` |
| sticky 우측 패널 | 해당 없음(1단이라 sticky 미적용) | 해당 없음 | 스크롤해도 우측 패널이 화면에 고정돼 있는지 `getBoundingClientRect`로 확인 | `01-crawl-run.md:28` |

## 02 수집 결과 (`/results`, Task 018A/018B 대기 — 화면 완성 후 검증)

| 확인 지점 | 375px | 768px | 1280px | 근거 |
| --- | --- | --- | --- | --- |
| 전체 레이아웃 | 목록·미리보기가 위-아래로 스택 | **`lg`(1024px) 미만이면 계속 스택** | **`lg`(1024px) 경계** — `lg:grid-cols-[360px_1fr]` 2단 | `02-collect-result.md:77,128,246,605` |
| 실행 요약 카드 그리드 | `grid-cols-1`(1열) | **`sm`(640px) 경계** — `sm:grid-cols-2`(2열)로 전환, 768px에서도 2열 유지 | 2열 유지 | `02-collect-result.md:139,546` |
| 파일 목록 형태 | `ul[role="listbox"] > li[role="option"]` 안 `button`(`lg:hidden`) | **`lg`(1024px) 미만이면 계속 리스트 형태** — 768px도 리스트인지 확인(04와 전환 기준이 다름 주의) | `Table`(`hidden lg:block`) | `02-collect-result.md:140,631,676` |
| 목록 ScrollArea 높이 | `max-h-[28rem]` 고정(반응형 변화 없음) | 동일 | 동일 | `02-collect-result.md:140,758` |

## 03 핫 키워드 분석 (`/keywords`, Task 021·022A/022B 대기 — 화면 완성 후 검증)

| 확인 지점 | 375px | 768px | 1280px | 근거 |
| --- | --- | --- | --- | --- |
| 조건 바 | `flex-col`(세로 1열 스택) | **`sm`(640px) 경계** — `sm:flex-row sm:items-end sm:flex-wrap`로 가로 배치 | 가로 배치 유지 | `03-hot-keyword.md:215,570` |
| 분석 요약 그리드 | `grid-cols-2` | **`sm`(640px) 경계** — `sm:grid-cols-3`, **`lg`(1024px) 경계** — `lg:grid-cols-5` | `grid-cols-5` | `03-hot-keyword.md:216,638` |
| Top 5 카드 그리드 | `grid-cols-2` + **375px에서는 유일하게 의도된 `overflow-x-auto snap-x` 가로 스크롤 허용**(5칸이 다 안 들어감 — 다른 화면의 "가로 스크롤 금지" 원칙의 예외) | `sm:grid-cols-3` | `lg:grid-cols-5` | `03-hot-keyword.md:180,205,217,685` |
| 랭킹 표 ↔ 카드 리스트 전환 | 카드 리스트(`keyword-rank-card-list.tsx`) | **`sm`(640px) 경계** — 표(`keyword-rank-table.tsx`)로 전환. **04(언론사)의 `md` 전환 기준과 다르다는 점 주의** | 표 유지 | `03-hot-keyword.md:80-81` |
| Top 5 카드 컨테이너 | 노출 | **`sm`(640px) 경계** — `hidden sm:block`으로 별도 처리된 컨테이너 존재 여부 확인 | 노출 | `03-hot-keyword.md:693` |

---

## 남은 리스크 · 다음 회차로 넘길 사항

- **이 문서는 사전 계획이다.** `docs/screens/playwright-scenarios.draft.md`가 이미 존재하는 것과 마찬가지로, 실제 구현이
  설계서 스켈레톤과 달라지면(특히 016A·018A·022A가 실제로 착수될 때) 이 문서의 "선행 Task" 열과 반응형 표를 함께 갱신해야 한다.
- **Task 025 자체가 이번 회차에 실행 대상이 아니다** — 위 "1부 집계"가 보여주듯 47/62 항목이 아직 대기 상태이므로, 이 리허설은
  "지금 무엇을 확인할 수 있는가"의 스냅샷일 뿐 Task 025의 조기 완료를 뜻하지 않는다. 다음 회차에 016A가 열리면 2단계·6단계 일부가,
  018A/018B가 열리면 3단계가, 022A/022B가 열리면 4·5단계와 R단계 전체가 순차적으로 "지금" 열로 바뀐다.
- **발견한 설계서 내부 불일치 1건**을 `docs/ISSUES.draft.화면.md`에 올렸다 — `01-crawl-run.md`의 언론사 목록 `ScrollArea`
  반응형 높이. 결정 문서(§스크롤 처리 결정)와 컴포넌트 명세 표는 모바일 `h-[320px]`를 요구하는데, 같은 문서의 마크업 스켈레톤
  예시 코드는 `h-[420px] pr-3 sm:h-[420px]`로 되어 있어 **모바일 전용 높이가 반영돼 있지 않다**. `docs/CONVENTIONS.md` §8이
  "마크업·라벨·상태는 설계서를 그대로 옮긴다"고 못 박은 만큼, Task 016A 담당자가 스켈레톤을 그대로 복사하면 프로즈가 요구하는
  반응형 동작을 놓칠 위험이 있어 미리 표시해 둔다.
- **012A/012B 완료분 회귀 확인 용도**: 5단계 표에 적었듯 5-3~5-5는 `/stopwords` 단독 진입으로 지금도 개별 확인 가능하다.
  Task 025 본번에는 포함되지 않지만, 이번 회차에 별도로 한 차례 눈으로 확인해 두면 022 완료 후 5단계 전체를 처음 태울 때
  회귀가 아니라 신규 결함만 걸러낼 수 있다.
